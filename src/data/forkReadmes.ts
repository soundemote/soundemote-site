// Auto-generated from raw READMEs fetched via raw.githubusercontent.com (master branch).
// Kept verbatim; relative asset/links are rewritten at render time in featuredArticles.ts.

export const ALIASING_WARS_MARKDOWN = "# ⚔️ soemdsp-sandbox 🛡️\n\n## Live Demo: http://soundemote.io/sandbox\n\nBrowser sandbox for trying `soemdsp` patching, generated artifacts, waveform\nviews, Render Sample, and Live Audio.\n\n## ⚔️💥 Aliasing wars: the Surge Oscillator 🛡️🧨\n\nThis branch (`aliasing-wars`) is a dedicated workspace for anti-aliased\noscillator work, starting with `native_modules/surge_oscillator` — a\nsaw/square/tri/sine oscillator with hard sync.\n\n> 🎚️ The name is a play on the [**loudness war**](https://en.wikipedia.org/wiki/Loudness_war) —\n> the decades-long race among mastering engineers to make recordings louder\n> and louder, at the cost of dynamic range. This branch is the same kind of\n> arms race, fought over a different quantity: not loudness, but how much\n> unwanted high-frequency garbage a digital oscillator sneaks in above\n> Nyquist. Same shape of fight, aliasing instead of loudness.\n\n**💣 The problem.** Hard sync forces a slave oscillator's phase back to 0 every\ntime a master signal crosses zero going up. That forced reset is a\ndiscontinuity injected mid-waveform — a little 🧨 detonating mid-cycle — and\nrendering it naively (just snapping `phase = 0`, with no correction) aliases\nbadly: 💥 the classic harsh, digital buzz under a sync sweep.\n\n**⚡🔧 The fix, in two parts:**\n\n1. **PolyBLEP correction reused, not reinvented.** This sandbox's existing\n   `polyblep.cpp` module already band-limits ordinary cycle wraps with a\n   PolyBLEP correction. A sync-forced reset and a natural wrap are the same\n   kind of event from the waveform function's point of view — phase lands\n   near 0 — so `surge_oscillator.cpp` reuses the identical\n   `polyBlep`/`polyBlepSquare`/triangle-integrator functions unchanged. Every\n   reset, sync-forced or natural, gets band-limited for free.\n2. **Sub-sample sync timing.** Sync input is read once per sample, but a real\n   zero-crossing can happen anywhere within that sample. Instead of always\n   resetting to exactly `phase = 0` (which quantizes sync timing to the\n   sample rate and adds its own jitter/aliasing at high sync ratios), the\n   module linearly interpolates the crossing time within the sample and\n   starts the new cycle already `frac` of the way in — the same idea Surge\n   and other analog-modeling synths use for sync-aware oscillators.\n\n**🔬📡 Verified, not assumed.** The compiled `.wasm` is tested against a\nPython + `wasmtime` harness exercising the real artifact directly (27\nassertions: pool exhaustion, waveform selection, level scaling, edge-triggered\nsync detection, and — the part that actually matters — proof that early vs.\nlate sync crossings within the same sample produce measurably different\noutput, confirming the sub-sample interpolation is doing real work and not a\nno-op).\n\n**Ports:** `0.1V/Oct` (pitch) and `Sync` (audio-rate signal; a rising\nzero-crossing triggers the reset) in; `Out` (the selected waveform), `Saw`,\n`Square`, `Tri`, `Sine` (always-on taps, like `polyblep.cpp`'s convention),\n`Synced` (a one-sample-wide pulse on the sample where a sync reset fired,\nfor chaining/visualizing), and `Internal Sync` (the built-in master\noscillator's raw signal, for inspection) out. Native C++/WASM with a JS\nfallback, wired into both the offline evaluator and the realtime audio\nworklet.\n\n**🚁🛩️ Built-in sync source.** Patching a real oscillator into `Sync` still\nworks, but most hard-sync sweeps don't need a second module just to get\none — the oscillator owns its own internal master oscillator (`Sync Freq`,\n0–20000 Hz, same range as the audible `Frequency`). With nothing patched\ninto `Sync`, the internal oscillator's zero-crossings drive the exact same\nsub-sample-interpolated reset path external audio would — a self-contained\nhard-sync sweep with two knobs and zero patch cables. Patch something into\n`Sync` and it takes over completely; the internal oscillator is a\nconvenience default, not an extra mandatory step.\n\n## 🌊⚡🔥 Aliasing wars: the BLIT Oscillator 🧨\n\nA third angle on the anti-aliasing mission: **BLIT** — Band-Limited Impulse\nTrain — a Stilson/Smith-style oscillator that generates a band-limited\nimpulse train and then *integrates* it into the classic waveshapes.\n\n> 🎚️ **Where it fits.** PolyBLEP corrects a discontinuity after it happens.\n> DSF never builds the discontinuity in the first place. BLIT builds the\n> waveform from a train of band-limited impulses that already respect the\n> Nyquist limit, then shapes it into saw, square, triangle, and sine by\n> integration — a different lineage, and a useful addition to the same\n> sandbox.\n\n**The closed form.** A single BLIT sample is the Dirichlet kernel:\n\n```blit(t, M) = sin(π · M · t) / (M · sin(π · t))```\n\nwhere `t` is phase in `[0, 1)` and `M = 2·⌊Nyquist / frequency⌋ + 1` is the\nodd number of harmonics that fit under half the sample rate. At the exact\nsingularity (`t = 0`) the limit is `1`, so the impulse peaks are handled by\na L'Hôpital branch, not by a literal impulse sample.\n\n**From impulses to waveforms.** The module keeps three leaky integrators:\n\n- **Saw** — one integration of the unipolar impulse train, with the DC ramp\n  (`dt`) subtracted so the integrator stays centered.\n- **Square** — one integration of a bipolar impulse pair (impulses at `t`\n  and `t + 0.5` with alternating sign).\n- **Triangle** — a second integration on top of the square output.\n\nA small amount of leakage (`0.999`–`0.9995`) keeps the integrators from\nwandering while preserving the waveform shape.\n\n**Pin-compatible with PolyBLEP.** The BLIT module exports the same tap\nlayout as the existing `polyblep.cpp` module: `Wave Out` (selected shape),\n`Saw`, `Ramp`, `Square`, `Tri`, and `Sine`. It can drop into the same node-\ngraph slots and patches, so it is easy to compare directly against the\nPolyBLEP and DSF approaches.\n\n**Native C++, freestanding WASM.** Unlike the PolyBLEP module, which uses\nEmscripten, the BLIT implementation is a self-contained C++ file with no\nstandard library dependencies. It compiles to a freestanding `wasm32` module\nwith no imports — only `memory` and the exported `soemdsp_blit_*` functions.\nThat makes it a lightweight reference for how a clean oscillator can be\nexposed to the sandbox's native-module bridge without runtime baggage.\n\n**Ports:** `0.1V/Oct` (pitch) and `Waveform` (0 = Saw, 1 = Ramp, 2 = Square,\n3 = Triangle, 4 = Sine) in; `Out`, `Saw`, `Ramp`, `Square`, `Tri`, `Sine`,\nand a version tap out. Native C++/WASM, wired into the offline evaluator and\nthe realtime audio worklet.\n\n## 🎛️⚡🔬 Alias-free oscillator study: the DSF technique 🧲\n\nStudied `C:\\Users\\argit\\Documents\\_PROGRAMMING\\soemdsp\\include\\soemdsp\\oscillator\\DSFOscillator.hpp`\n(Walter Hackett's alias-free oscillator) as a second angle on the aliasing\nmission, distinct from PolyBLEP.\n\n> 🔍 **A note on attribution.** No public record turns up connecting a\n> \"Walter Hackett\" to DSF synthesis or alias-free oscillator design — the\n> technique itself is academically documented back to **James A. Moorer's**\n> 1975/76 Stanford CCRMA work, *\"The Synthesis of Complex Audio Spectra by\n> Means of Discrete Summation Formulas,\"* and the derivation below is\n> Moorer's. But the connection here is personal, not academic: **Walter\n> Hackett is who introduced this concept**, the person this implementation's\n> lineage actually traces back to for the team working on it — that's a real\n> and separate thing from who first published the math, and both are true\n> at once.\n\n**The core idea is fundamentally different from PolyBLEP.** PolyBLEP starts\nfrom a naive discontinuous waveform (a hard saw/square edge) and *corrects*\nthe discontinuity after the fact with a band-limited step function. DSF\n(Discrete Summation Formula) synthesis never generates the discontinuity in\nthe first place — it computes the waveform directly from a **closed-form\ntrigonometric sum** of a bounded number of harmonics (`numPartials_ =\nNyquist / frequency`, recalculated on every frequency change). Because the\npartial count is derived from the Nyquist limit, the waveform is alias-free\n*by construction* — there's nothing above Nyquist to alias, rather than\nsomething being suppressed after the fact.\n\n**What's in the file:**\n- `DSFOscillatorBase` — shared machinery: a phase accumulator (`calculateState()`),\n  a leaky integrator (`leak_`) that fades in the amplitude-adjusted output\n  over time (looks aimed at taming attack transients), and a `Wire`-based\n  parameter system (`pointTo()`/`slave()`) that lets multiple oscillator\n  instances share phase and morph state — a lightweight master/slave\n  patch-cable primitive, conceptually similar to this sandbox's node wires\n  but scoped to parameter sharing rather than the whole graph.\n- `DSFOscillatorSineSaw` — continuously morphs sine → saw via a single\n  `morph_` parameter (0–1), which reshapes a `k_`/`k2_`/`k42_` coefficient\n  set feeding the closed-form DSF sum.\n- `DSFOscillatorSineSquare` — same idea, sine → square, with its own\n  coefficient derivation and partial-count halving (`/ 2.0`).\n\n### 🧮⚙️ How the equation was derived 🔩\n\n<div align=\"center\">\n<img src=\"/media/aliasing-wars/dsf-derivation.svg\" alt=\"Four-step derivation: an infinite geometrically-decaying harmonic sum is rewritten as a complex exponential, collapsed by the geometric series identity, and reduced to one closed-form trig equation\" width=\"90%\"/>\n</div>\n\nThe derivation is genuinely elegant, and the trick is one line of algebra\ndoing all the work:\n\n1. 🎵 **Start with the sound you actually want** — infinitely many harmonics,\n   each quieter than the last by a fixed ratio `a` (0 ≤ a < 1):\n   `y(θ) = Σ aⁿ·sin((n+1)θ)` for `n = 0…∞`. This is a real, audible,\n   band-unlimited signal — completely impractical to compute directly,\n   since it's an infinite sum.\n2. 🌀 **Rewrite it with complex exponentials.** Euler's formula\n   (`e^{ix} = cos x + i·sin x`) turns each `sin` term into the imaginary\n   part of a complex exponential, and — this is the useful part — turns the\n   whole sum into `Σ aⁿ e^{i(n+1)θ}`, which factors into\n   `e^{iθ} · Σ (a·e^{iθ})ⁿ`.\n3. 💥 **The geometric series identity collapses it.** `Σ rⁿ = 1/(1−r)` for\n   any `|r| < 1`, summed to infinity — one of the oldest identities in\n   algebra. Substituting `r = a·e^{iθ}` turns the *infinite sum* into *a\n   single fraction*, no loop, no series, nothing left to add up.\n4. ✅ **Take the imaginary part and you have your closed-form oscillator.**\n   What comes out is one trigonometric expression in `θ`, `a`, and the\n   partial count — exactly the shape of the `DSF()` function in the code\n   (`k_` is `a`, `numPartials_` is the harmonic count, `dsfState_` is `θ`).\n   Every sample, the oscillator evaluates that one closed-form line instead\n   of summing any harmonics at all — which is *also* why it's fast: the\n   \"summation\" in Discrete Summation Formula happened once, on paper, in\n   1976, not once per sample at runtime.\n\nThe alias-free property falls out of the same math: because the harmonic\ncount feeding the closed form is derived from `Nyquist / frequency`, the\nformula only ever represents harmonics that fit under Nyquist. There's\nnothing above the limit to alias in the first place — the geometric series\nidentity that makes the equation *fast* is the same one that makes it\n*clean*.\n\n**The file is honest about its own problems** — the header comment block\nlists them directly: attack causes an amplitude spike, volume is\ninconsistent across `morph_` and across frequency, harmonics visibly \"click\"\nin and out as frequency rises (consistent with `numPartials_` changing in\ninteger-ish steps with no smoothing between values), the saw/square volumes\ndon't match each other, and square gets dull at low frequency. None of these\nare aliasing bugs — DSF's alias-free guarantee holds regardless — they're\namplitude-normalization and transient issues layered on top of a\nmathematically sound core.\n\n**Takeaway for this mission:** PolyBLEP (what Surge Oscillator uses) and DSF\nsolve the same problem from opposite directions — correct the edge vs. never\ncreate the edge — and the tradeoffs are different too: DSF needs a live\npartial-count recalculation per frequency change (cheap, but is exactly\nwhere this implementation's harmonic \"clicking\" comes from), while PolyBLEP\nneeds a correction at every phase discontinuity, natural or sync-forced,\nwhich is what `surge_oscillator.cpp` already does. A DSF-based module here\nwould be a genuinely different oscillator, not a redundant one — noted as\na real option for future work, not built in this pass.\n\n📚 **Source:** Moorer, J. A. (1976). *The Synthesis of Complex Audio Spectra\nby Means of Discrete Summation Formulas.* Stanford CCRMA (STAN-M-5).\n\n## 🧪🔋 The DSF starter kit ⚡🚀\n\n`native_modules/dsf_oscillator` — Native C++/WASM with a JS fallback,\nwired into both the offline evaluator and the realtime audio worklet,\nsame as Surge Oscillator. This got rewritten several times before\nlanding here (that history is preserved further down, past the license,\nfor anyone who wants the debugging trail); this section explains the\noscillator as it actually works today.\n\n### ⚙️🔋 The core idea: one formula, one control, one accumulator\n\nEverything in this module comes from a single closed form, transcribed\ndirectly from `pureSawEng` in Walter H. Hackett's \"Extended DSF\nOscillators.cxx\":\n\n```\npureSawEng(t, N) = sin(π·t·(2N + 1)) / sin(π·t)  −  1\n```\n\n`t` is phase in `[0, 1)`; `N` is a harmonic count. This is **not**\nevaluated directly as the output waveform. It's run through a leaky\nintegrator — the same accumulator pattern used throughout this family of\noscillators (`DSFOscillatorBase::run()` in the original header does the\nsame thing):\n\n```\nvalue = value · retention  +  pureSawEng(t, N) · dt\n```\n\nwhere `dt = frequency / sampleRate` and `retention` decays the\naccumulator's memory across roughly 20 periods of the current pitch (not\na fixed sample count — a fixed retention forgets mid-ramp at low\nfrequencies and distorts the shape; see the history below for how that\nwas found). Treat `pureSawEng` as a *rate of change*, and the accumulator\nas the thing that turns that rate into an actual waveform, the way\nintegrating a square wave produces a triangle wave.\n\n`N` is always capped at `⌊Nyquist / frequency⌋` — the most harmonics\nthat can fit under the sample rate for the current pitch. That's what\nmakes every waveform in this module alias-free *by construction*: it is\nstructurally impossible to ask for more harmonics than fit.\n\n**Harmonics (0–1)** is the one knob that shapes every waveform's\ncharacter. It crossfades `N` continuously from 1 up to that Nyquist-safe\nmaximum, blending between the two nearest integer harmonic counts so the\nsweep is smooth rather than stepped. It currently displays as a raw\n`0.000`–`1.000` fraction rather than a literal harmonic-count number.\n\n### 🛠️⚡ Every waveform is a variation on that one accumulator\n\n- **Sine** — `sin(2π·t)` directly. No DSF math, no accumulator; the true\n  floor of the instrument.\n- **Saw** — `pureSawEng`, Harmonics-morphed, run through the accumulator\n  above. This is the one real reference formula everything else is built\n  from.\n- **Square (PWM)** — `saw(t) − saw(t − pulseWidth)`. Subtracting a\n  phase-shifted copy of the already-correct Saw is itself alias-free —\n  no new formula, no new singularity to worry about — and `pulseWidth`\n  (0–1) sets duty cycle the way PWM does on any analog square. Its\n  higher harmonics keep its peak swing roughly constant as duty cycle\n  narrows.\n- **Trimorph** *(was called Triangle)* — a **second** leaky integration\n  on top of Square's output. Integrating a square produces a triangle;\n  integrating it a second time here is literally what turns Square's\n  edges into Trimorph's ramps. Because this stage mostly tracks Square's\n  *fundamental* harmonic, and a PWM pulse train's fundamental amplitude\n  genuinely scales with `sin(π·dutyCycle)`, Trimorph is compensated by\n  dividing by that same factor before its own peak-follower normalizer —\n  otherwise it fades to silence as `pulseWidth` approaches 0 or 1.\n- **SquSaw** *(was called TriMorph)* — a plain crossfade between Saw and\n  a *fixed* 50%-duty Square (deliberately not tied to the `pulseWidth`\n  knob), landing on a saw-to-triangle-like character rather than a\n  saw-to-square one. One knob, `blend` (0–1): 0 is pure Saw, 1 is pure\n  50%-duty Square.\n\n### 🎯🪖 Getting the four classic waveshapes\n\n| Shape | Waveform | Harmonics | PWM / Blend |\n|---|---|---|---|\n| **Sine** | `Sine` (or any other waveform with Harmonics = 0) | — | — |\n| **Sawtooth** | `Saw` | `1.0` | — |\n| **Square** | `Square (PWM)` | `1.0` | `pulseWidth = 0.5` |\n| **Triangle** | `Trimorph` | `1.0` | `pulseWidth = 0.5` |\n\n`SquSaw` is the bonus fifth shape this technique makes easy — a\ncontinuous morph *between* two of the classics, not one of them.\n\n### 🌊⚡🔬 Why reducing everything to a sine is the interesting part\n\nEvery non-Sine waveform in this module collapses to an **exact sine**\nat `Harmonics = 0` — not an approximation, not a \"close enough\" sine,\nthe literal same single-harmonic closed form regardless of which\nwaveform you started from. That's a direct, audible consequence of the\narchitecture: every waveform shares the same underlying `pureSawEng`\nharmonic machinery, just fed through a different post-processing stage\n(a subtraction for Square, a second integration for Trimorph, a\ncrossfade for SquSaw) — so winding Harmonics down doesn't cross-fade to\nsome *other* fixed sine, it un-builds the exact same harmonic sum every\nother waveform is made of, one harmonic at a time, down to the single\nfundamental they all share.\n\nThat's what makes sweeping Harmonics on this oscillator sound organic\nrather than mechanical: a classic wavetable morph crossfades between two\n*independently-drawn* shapes, so the midpoint is a blend of two unrelated\ntimbres. Here, every point along the Harmonics knob is the *same*\nwaveform with fewer or more harmonics — brightness and body come from\nliterally adding or removing overtones from one continuous structure,\nthe way a real acoustic instrument's harmonic content actually changes\nwith dynamics or technique, not the way a synthesizer cross-fader does.\nCombine that with Square's PWM, Trimorph's triangle-ness, or SquSaw's\nSaw/Square blend, and Harmonics becomes a second axis of movement layered\non top — which is where this stops being \"four classic waveshapes\" and\nstarts being a genuinely novel timbral space: PWM-narrow squares thinning\ndown toward a sine at low Harmonics, Trimorph ramps softening into a pure\ntone, SquSaw hybrids that are neither saw nor square nor triangle at any\nsingle setting. None of that palette exists in a standard \"pick a\nwaveform, cross-fade to the next\" oscillator.\n\n## License\n\nThis repository is source-available for noncommercial use only. Commercial use\nrequires a separate written commercial license from Soundemote. See\n[`LICENSE`](LICENSE).\n";
export const ALIASING_WARS_REPO = "elanhickler/soemdsp-sandbox-aliasing-wars";

export const ANALOG_FILTERS_MARKDOWN = "<div align=\"center\">\n\n# 🎚️ soemdsp-sandbox — Analog Filters\n\n*A fork of [soemdsp-sandbox](https://github.com/soundemote/soemdsp-sandbox),*\n*chasing the sound of circuits that were never digital to begin with.*\n\n[![License: Noncommercial](https://img.shields.io/badge/license-noncommercial-blue.svg)](LICENSE)\n[![Language: C++/WASM](https://img.shields.io/badge/native-C%2B%2B%20%E2%86%92%20WASM-654ff0.svg)](native_modules)\n[![Runtime: Vanilla JS](https://img.shields.io/badge/runtime-vanilla%20JS-f7df1e.svg)](public)\n[![Status: Research](https://img.shields.io/badge/status-research-orange.svg)](#)\n[![Focus: Analog Filters](https://img.shields.io/badge/focus-analog%20filters-654ff0.svg)](#)\n\n</div>\n\n---\n\n## 📖 Contents\n\n- [Why analog filters](#-why-analog-filters)\n- [What makes them hard to get right in software](#-what-makes-them-hard-to-get-right-in-software)\n- [Built: the Flower Child family](#-built-the-flower-child-family)\n- [Characterizing behavior empirically](#-characterizing-behavior-empirically)\n- [Filters on the list](#-filters-on-the-list)\n- [Listen & watch](#-listen--watch)\n- [Running it](#-running-it)\n- [License](#-license)\n\n---\n\n## 🎛️ Why analog filters\n\nEvery classic analog filter is a physical accident wearing a transfer\nfunction. Resistors, capacitors, and transistors doing exactly what physics\ndemands of them — and what physics demands turns out to sound *incredible*\nunder stress: self-oscillating resonance, soft-clipping feedback loops,\ncomponent drift, asymmetric distortion on the way into saturation. None of\nthat is a bug. It's the entire reason a Moog ladder filter has a personality\nand a textbook biquad doesn't.\n\nThis fork exists to chase that personality in `soemdsp`'s native C++/WASM\nmodules — not by approximating the *sound* from the outside, but by modeling\nthe *circuit* closely enough that the personality falls out on its own.\n\n---\n\n## 🧪 What makes them hard to get right in software\n\nA naive digital filter is linear, time-invariant, and stable by construction.\nA real analog filter is often none of those things, which is exactly what's\nbeing chased here:\n\n- **Nonlinearity.** Real transistor ladder stages saturate. A textbook\n  digital filter doesn't, unless you deliberately put a nonlinearity back in\n  — and where you put it changes the sound completely.\n- **Self-oscillating resonance.** Push feedback gain high enough on a real\n  Moog ladder and it turns into an oscillator, cleanly, on purpose. Getting\n  a digital model to do the same without exploding numerically is the whole\n  game.\n- **Zero-delay feedback.** Naive digital translations of analog feedback\n  loops introduce a one-sample delay that isn't in the real circuit, which\n  audibly changes the resonant behavior. Topology-preserving transform (TPT)\n  / zero-delay-feedback (ZDF) techniques exist specifically to close that gap.\n- **Frequency-dependent nonlinear behavior.** Saturating a signal *before*\n  filtering it sounds different from saturating *after* — and real circuits\n  often do both, in a loop, simultaneously. That interaction is where a lot\n  of the \"expensive analog gear\" character actually lives.\n- **Aliasing from the nonlinear stages.** Any saturation stage generates\n  harmonics; without oversampling, those harmonics fold back down as\n  aliasing. Getting the nonlinear modeling right and getting the aliasing\n  under control are two separate problems that have to be solved together.\n\n---\n\n## 🌸 Built: the Flower Child family\n\nPorted from an older `soemdsp` codebase (`FlowerChildFilterCore.h`) —\nresonant, self-oscillating feedback designs, not passive filters in the\ntextbook sense. Each is a native C++/WASM module, verified against the real\ncompiled artifact with a Python+wasmtime harness before wiring:\n\n| Module | Modes | Notes |\n|---|---|---|\n| `flower_child_filter` | Clean, Dirty, Rev3, Downsampled | The original two revisions plus an ellipsoid-oscillator variant and a sample-and-hold aliasing variant |\n| `rsmet_filter` | LP6/12/18/24, HP6/12/18/24, BP6, BP12 (10) | A ladder filter with a tanh soft clipper and noise injection stage |\n| `yellowjacket_filter` | — | Feedback ellipse-oscillator filter, grindy, easily produces square-wave-like output. Its resonance has a chaotic, bubbly character reminiscent of a Polivoks-style filter |\n| `superlove_filter` | LP18, LP24, HP6, BP6 | Trisaw-oscillator feedback resonator, warm and stably self-oscillating |\n| `chaotic_phase_locking_filter` | — | Direct feedback ellipse-waveshaper resonator (no oscillator phasor) |\n| `resonator_filter` | Sinusoid, Triangle, Sawtooth | Dual-phasor FM feedback resonator — each mode's *resonance itself* is visibly and audibly shaped like its namesake, not just a generic buzz with a different label. See below |\n| `human_filter` 🚧 | BP6, LP6, LP12 | Dual-phasor feedback network shaped by a bell filter — marked under construction; the original's feedback-filter wrapper (Q, center frequency) wasn't recoverable from the accessible codebase, so a documented Q=1/1kHz default stands in |\n\nEvery shaping curve in these (resonance-vs-frequency, FM/PM crossfade, etc.)\nis reproduced from the real `soemdsp::utility::Graph` /\n`soemdsp::curve::Rational` source, not approximated — a generic N-node graph\nevaluator was built once and reused across all of them.\n\n**What makes Flower Child Filter itself interesting:** its Dirty/Rev3-style\noscillators don't crossfade between a sine and a square wave with two\nseparate waveshapers — they use one continuous\n[`ellipse()`](https://github.com/soundemote/oldcode/blob/main/old%20stuff%20se_framework/SynthesizerComponents/oscillator/waveshapes.cpp)\nfunction that morphs a sine into a square (and everywhere in between) as a\nsingle parameter moves, driven directly by resonance. That's the actual\nmechanism behind why turning resonance from clean to hot doesn't feel like\nswitching between two different sounds — it's one continuous, stable\nwaveshape sweep behind the feedback loop, which is exactly why it sounds and\nbehaves like a real overdriven self-oscillating filter rather than a\ndigital effect being crossfaded in.\n\n**SuperLove's HP6 mode in particular** screams — driven hard, it produces\nclean, beautiful square waves and is generally one of the hottest-sounding\nhighpass filters in this set.\n\n**Resonator Filter deserves more than \"dual-phasor FM feedback resonator.\"**\nWhat actually makes it interesting is that each of its three modes doesn't\njust change the *timbre* of the resonance — it visibly reshapes what the\nresonance *is*:\n\n- **Sawtooth mode's** resonance is literally sawtooth-shaped when you look\n  at the waveform, not just \"a buzzier tone.\"\n- **Triangle mode's** resonance is literally triangular — a different\n  geometric shape entirely, not a filtered version of the same shape.\n- **Sinusoid mode's** resonance looks like an overly rounded sine wave, and\n  that rounding is exactly why it sounds bubbly rather than smooth — a kind\n  of sinusoidal fractal quality that comes directly from the shape, not from\n  added modulation.\n\nThat's a genuinely novel result for a feedback resonator: the *shape* of the\nself-oscillation is the mode, not a label on top of the same underlying\nwaveform. Measured directly from the real compiled `.wasm` (driven with a\n220Hz tone at resonance 0.7–0.85, steady state):\n\n<div align=\"center\">\n<img src=\"/media/analog-filters/resonator-waveforms.png\" alt=\"Three stacked waveform plots: Sinusoid mode showing a rounded sine, Triangle mode showing a triangular shape with jitter, and Sawtooth mode showing an asymmetric ramp-and-decay shape\" width=\"85%\"/>\n</div>\n\n---\n\n## 📈 Characterizing behavior empirically\n\nHere's the thing that makes this whole family hard to reason about from the\ncode alone: **they're feedback oscillators, not fixed filters.** A plain\nlowpass has one transfer function you can write down. These don't — the\n\"curve\" depends on resonance, input level, and the knob position feeds back\ninto the oscillator's own pitch. There's no formula to graph.\n\nSo instead of guessing, the plan is to *measure*: feed a compiled module a\nswept sine tone through the same Python+wasmtime harness already used to\nverify it, record output RMS per frequency, and plot the result. This turns\n\"what does turning this knob actually do\" from a guess into a chart.\n\n**First result, `yellowjacket_filter`** (see the module's own naming\nconfusion first — `Yellowjacket_BP` in the original code, but the filter\ntap it actually uses is `LP_6`, a lowpass): swept a sine tone from 20Hz to\n14kHz through the compiled `.wasm` at three Frequency-knob settings,\nresonance fixed at 0.3:\n\n- **Knob 0.2** — flat response (~0.616 RMS) across the whole sweep. The\n  self-oscillation is loud enough to drown out whatever's coming in; the\n  input frequency barely matters.\n- **Knob 0.5** — behaves like an actual lowpass: loud below ~100Hz, settling\n  to ~0.046 above ~400Hz.\n- **Knob 0.8** — a genuine resonant peak around 1.2–1.6kHz (~0.21 RMS,\n  roughly double its neighbors), falling off on both sides.\n\n<div align=\"center\">\n<img src=\"/media/analog-filters/yellowjacket-response.png\" alt=\"Line chart of Yellowjacket Filter output level versus input frequency (log scale) at three Frequency knob settings. Knob 0.2 is flat. Knob 0.5 slopes down like a lowpass. Knob 0.8 has a clear resonant peak around 1.2 to 1.6kHz.\" width=\"85%\"/>\n</div>\n\nThat last point is the answer to \"but it sounds like a bandpass in use\" —\nit does, and now there's a measurement showing exactly where and how much.\nThe lesson generalizes: for this whole family, \"what's the filter curve\"\nonly has an honest answer as a measured, knob-position-dependent chart, not\na static formula — and that's the method to reach for on the rest of the\nlist below as they get built.\n\n---\n\n## 🎚️ Filters on the list\n\n| Filter | Status |\n|---|---|\n| **Moog Ladder** (4-pole transistor ladder, self-oscillating resonance) | 🔲 not started |\n| **Diode Ladder** (TB-303-style, asymmetric diode nonlinearity) | 🔲 not started |\n| **Sallen-Key** (2-pole op-amp topology, gentler slope) | 🔲 not started |\n| **State-Variable Filter** (simultaneous LP/HP/BP/notch outputs) | 🔲 not started |\n| **Twin-T Notch** (passive notch, the basis of classic phaser/wah circuits) | 🔲 not started |\n| **Discrete Multimode Filter** (parallel 24dB LP / 24dB HP / 12dB BP / 12dB notch outputs, resonance from a feedback loop with an insert point in the path) | 🔲 not started |\n| **Simultaneous LP/HP Filter** (one core filter stage driven as a 12dB lowpass and a separate highpass at once, each with its own audio input and level control, prized for a screaming self-oscillating character) | 🔲 not started |\n| **Switchable Third-Order Filter** (three cascaded first-order sections, each switchable between lowpass and highpass, a mode switch selecting among four low-pass/band-pass/reversed-band-pass/high-pass combinations, and a voltage-controlled resonance amplifier that can be driven well past the onset of oscillation into chaotic and phase-locked territory, with taps available after each of the three stages) | 🔲 not started |\n| **Diode-Controlled LP/HP Pair** (a highpass stage tracking at half rate paired with a lowpass stage tracking at full rate to form a bandpass-like sweep, with frequency set by diode control current rather than a transistor or OTA stage — which naturally narrows the usable sweep range — and matched capacitor pairs tuning the corner behavior) | 🔲 not started |\n\nThis table is the honest state of things: a target list, not a changelog.\nEach filter gets the same treatment already proven out elsewhere in\n`soemdsp-sandbox` — native C++ compiled to WASM, verified against the real\ncompiled artifact (not just a JS mirror) before it's wired into the graph.\n\n---\n\n## 🎧 Listen & watch\n\n*(Flower Child Filter has a real demo below; the rest are placeholder links —\nreal recordings and demo videos go there once they exist.)*\n\n| Filter | Audio example | Video demo |\n|---|---|---|\n| **Flower Child Filter** (rev.1) | — | [🖤 Flower Child Filter rev.1 self oscillation test 🌸](https://www.youtube.com/watch?v=m-_bo7PFUGI) |\n| Moog Ladder | [File — TBD](https://drive.google.com/drive/folders/REPLACE_ME_MOOG_LADDER_AUDIO) | [Video — TBD](https://youtube.com/watch?v=REPLACE_ME_MOOG_LADDER_DEMO) |\n| Diode Ladder | [File — TBD](https://drive.google.com/drive/folders/REPLACE_ME_DIODE_LADDER_AUDIO) | [Video — TBD](https://youtube.com/watch?v=REPLACE_ME_DIODE_LADDER_DEMO) |\n| Sallen-Key | [File — TBD](https://drive.google.com/drive/folders/REPLACE_ME_SALLEN_KEY_AUDIO) | [Video — TBD](https://youtube.com/watch?v=REPLACE_ME_SALLEN_KEY_DEMO) |\n| State-Variable Filter | [File — TBD](https://drive.google.com/drive/folders/REPLACE_ME_SVF_AUDIO) | [Video — TBD](https://youtube.com/watch?v=REPLACE_ME_SVF_DEMO) |\n| Twin-T Notch | [File — TBD](https://drive.google.com/drive/folders/REPLACE_ME_TWIN_T_AUDIO) | [Video — TBD](https://youtube.com/watch?v=REPLACE_ME_TWIN_T_DEMO) |\n| Discrete Multimode Filter | [File — clean filter, hot growl](https://drive.google.com/file/d/1E3-sMArwa7t_eC6wMtEOVAn5BaVc_leS/view?usp=drive_link) | [Video — TBD](https://youtube.com/watch?v=REPLACE_ME_DISCRETE_MULTIMODE_DEMO) |\n| Simultaneous LP/HP Filter | [File — audio demo](https://drive.google.com/file/d/1v6cj6S2RXMOlhOBtbkLipTRUmtfrA46H/view?usp=drive_link) | [Video — TBD](https://youtube.com/watch?v=REPLACE_ME_SIMULTANEOUS_LPHP_DEMO) |\n| Switchable Third-Order Filter | [Demo 1](https://drive.google.com/file/d/1bhXlDZkRRuVh6U2f-yfDGbNIiGXiXShG/view?usp=drive_link) · [Demo 2](https://drive.google.com/file/d/1n_9JrZ-zFQ6GQ_a3WGlaKEBpWaulGuDD/view?usp=drive_link) · [Demo 3](https://drive.google.com/file/d/17c3guemmtnHMpqAFP10LeAs0udspJS4r/view?usp=drive_link) · [Demo 4](https://drive.google.com/file/d/1qEJnqQwlNJC80FcRapuWH1bSFhWHdRDQ/view?usp=drive_link) | [Video — TBD](https://youtube.com/watch?v=REPLACE_ME_SWITCHABLE_THIRD_ORDER_DEMO) |\n| Diode-Controlled LP/HP Pair | [File — audio demo](https://drive.google.com/file/d/1fkqbuZDtS1OKaCmWBK-u9vtAbCAzDxhS/view?usp=drive_link) | [Video — TBD](https://youtube.com/watch?v=REPLACE_ME_DIODE_CONTROLLED_LPHP_DEMO) |\n\n---\n\n## 📄 License\n\nSource-available for noncommercial use only, same as upstream. Commercial use\nrequires a separate written commercial license from Soundemote. See\n[`LICENSE`](LICENSE).\n\n<div align=\"center\">\n\n*Chasing circuits that were never digital to begin with.*\n\n</div>\n";
export const ANALOG_FILTERS_REPO = "elanhickler/soemdsp-sandbox-analog-filters";

export const DIGITAL_EFFICIENT_PATCH_SYSTEM_MARKDOWN = "<div align=\"center\">\n\n# 🔥 Efficient Multiplayer Patch Editing 🔥\n\n# ⚙️ soemdsp-sandbox — Digital Efficient Patch System\n\n*A fork of [soemdsp-sandbox-digital-signals-audio](https://github.com/elanhickler/soemdsp-sandbox-digital-signals-audio),*\n*narrowing focus to one question:*\n**how fast can loading, saving, and editing a patch be — and can multiple people edit the same one safely?**\n\nThis fork's mandate is the patch system specifically: load/save/edit speed\nand the data model that supports it, including real multiplayer editing.\nSIMD-readiness for the audio DSP engine is a separate agent's scope — this\nwork tries not to obstruct it, but doesn't claim to deliver it.\n\n[![License: Noncommercial](https://img.shields.io/badge/license-noncommercial-blue.svg)](LICENSE)\n[![Language: C++/WASM](https://img.shields.io/badge/native-C%2B%2B%20%E2%86%92%20WASM-654ff0.svg)](native_modules)\n[![Runtime: Vanilla JS](https://img.shields.io/badge/runtime-vanilla%20JS-f7df1e.svg)](public)\n[![Status: Investigating](https://img.shields.io/badge/status-investigating-orange.svg)](#)\n[![Goal: Multiplayer-ready](https://img.shields.io/badge/goal-multiplayer--ready-654ff0.svg)](#)\n\n</div>\n\n---\n\n## 📖 Contents\n\n- [Why this fork exists](#-why-this-fork-exists)\n- [Online collaboration & multiplayer — current state](#-online-collaboration--multiplayer--current-state)\n- [The real bottleneck (it isn't file size)](#-the-real-bottleneck-it-isnt-file-size--measured-not-guessed)\n- [Phase 4, round 1: the heatmap fix](#-phase-4-round-1-the-heatmap-was-forcing-layout-on-every-edit)\n- [Phase 4, round 2: a correct fix that didn't help](#-phase-4-round-2-a-correct-fix-that-turned-out-not-to-matter-reported-honestly)\n- [Phase 4, round 3: why the layer read count wasn't the cost](#-phase-4-round-3-a-real-finding-and-why-it-stops-here-for-now)\n- [Phase 4, round 4: throttling beat caching](#-phase-4-round-4-the-real-fix-wasnt-caching-it-was-throttling)\n- [The layering this has to respect](#-the-layering-this-has-to-respect)\n- [The proof ladder](#-the-proof-ladder)\n- [Where serialized connections meet SIMD and video](#-where-serialized-connections-meet-simd-and-video)\n- [Diff-friendly online collaboration (corrected)](#-the-same-shape-also-buys-diff-friendly-online-collaboration--with-a-correction)\n- [Phase 6: a real multiplayer merge engine](#-phase-6-an-actual-multiplayer-merge-engine)\n- [Phase 6, round 2: applying a merge live](#-phase-6-round-2-applying-a-merge-to-the-live-app-without-crashing-it)\n- [Phase 6, round 3: the actual network hop](#-phase-6-round-3-the-actual-network-hop)\n- [Phase 6, round 4: wiring real slider drags](#-phase-6-round-4-wiring-it-to-real-slider-drags)\n- [Phase 6, round 5: throttling the broadcast rate](#-phase-6-round-5-a-slider-drag-was-broadcasting-once-per-pixel)\n- [Plan of attack](#-plan-of-attack)\n- [Running it](#-running-it)\n- [License](#-license)\n\n---\n\n## 🎯 Why this fork exists\n\nThe question that started this: *\"what's the best compression format we can do for our\npatch system so loading/saving/editing is faster?\"*\n\nThe honest answer turned out to be a redirect, not a format: today's patches\n(`saved-patches/*.json`) are 1.6–18 KB of pretty-printed JSON. That's not a\ntransfer-speed problem — minifying or gzipping it would save milliseconds nobody\nwould notice. If saving, loading, or editing *feels* slow, the far more likely\nculprit is what happens **after** the JSON is parsed: rebuilding the in-memory\ngraph, re-running normalizers, and reconstructing the execution plan every time a\npatch changes.\n\nSo this fork's actual mission is two-layered:\n\n1. **Short term:** profile and speed up the in-memory graph-rebuild step — the\n   part that runs on every load, save, and edit, regardless of file format.\n2. **Long term:** shape the graph's data model so it holds up under real\n   multiplayer editing — stable node identity, a merge engine that actually\n   converges, a transport to carry changes between sessions.\n\n---\n\n## 🤝 Online collaboration & multiplayer — current state\n\n**Short version: two people can edit the same patch, on two different\nmachines, and both sets of changes land correctly — proven with a real\nmerge engine talking over a real network hop, not assumed.** Dragging a\nparameter slider now actually broadcasts, and a background loop applies\nwhatever comes back automatically — every piece of the pipeline has been\nbuilt and independently verified.\n\n| Layer | What it does | File | Status |\n|---|---|---|---|\n| Data shape | Nodes keyed by stable id, not array position | `node-graph-patch-serialization.js` / `node-graph-patch-core.js` | ✅ Phase 5 |\n| Merge engine | Last-writer-wins per field, tombstones for delete | `node-graph-patch-lww-merge.js` | ✅ Phase 6 round 1 |\n| Live application | Merged result → the actual running editor, safely | `node-graph-patch-lww-live.js` | ✅ Phase 6 round 2 |\n| Transport | HTTP polling relay, broadcast + poll | `server.py`, `node-graph-patch-lww-transport.js` | ✅ Phase 6 round 3 |\n| Editor wiring | Slider drags auto-broadcast; remote edits auto-apply | `node-graph-patch-lww-editor-wiring.js` | ✅ Phase 6 round 4 (sliders only — see below) |\n\n**Why the merge is actually trustworthy, not just \"seems to work\":** the\nmerge engine was tested against the three mathematical properties that\nguarantee two people's editors converge on an identical result no matter\nwhat order network messages arrive in, or whether one gets delivered\ntwice — commutative, associative, idempotent. All three verified directly.\nSee [Phase 6](#-phase-6-an-actual-multiplayer-merge-engine) for the actual\ntest results.\n\n**Try it right now**, from a browser console with the app loaded — this is\nthe real, tested, currently-shipping behavior, not pseudocode:\n\n```js\n// Tab A joins a session, then just drags a slider like normal:\nstartNodeGraphLwwMultiplayerSession(\"my-session\");\n// ...drag any parameter slider in the UI — it broadcasts automatically.\n\n// Tab B (a different browser/machine) joins the same session and its\n// background poll picks up Tab A's edit within ~500ms, no further calls needed:\nstartNodeGraphLwwMultiplayerSession(\"my-session\");\n```\n\n**What's honestly still missing:**\n\n- Only parameter sliders are wired — node position (drag-to-move) and node\n  create/delete don't broadcast yet.\n- No join/room UI — a session is started by calling\n  `startNodeGraphLwwMultiplayerSession(id)` directly; there's no button for it.\n- No presence (no \"who else is editing this patch right now\").\n- No reconnect/resume — a dropped connection loses its place in the poll\n  sequence.\n- Timestamps are plain wall-clock numbers — safe on one machine, not yet\n  safe across machines with different system clocks (needs a logical clock).\n- The transport is a proof-scale in-memory relay (`server.py`), not\n  production infrastructure — sessions vanish on server restart.\n\nFull details, including every test that was actually run, are in\n[Phase 6](#-phase-6-an-actual-multiplayer-merge-engine),\n[round 2](#-phase-6-round-2-applying-a-merge-to-the-live-app-without-crashing-it),\n[round 3](#-phase-6-round-3-the-actual-network-hop),\nand [round 4](#-phase-6-round-4-wiring-it-to-real-slider-drags) below.\n\n---\n\n## 🔍 The real bottleneck (it isn't file size — measured, not guessed)\n\nBefore touching any serialization format, Phase 1 measured the actual pipeline\nlive in the browser, timing every stage of loading and committing the largest\ndemo patch (`lorenz-demonstration`, 17.6 KB, 7 nodes) with `performance.now()`,\nmedian of 200 runs per stage:\n\n| Stage | Median time |\n|---|---|\n| `JSON.parse` | **~0.0 ms** |\n| `validateNodeGraphPatch` | **~0.1 ms** |\n| `compileNodeGraphExecutionPlan` | **~0.1 ms** |\n| `serializeNodeGraphPatch` | **~0.0 ms** |\n| **Full `commitNodeGraphPatch` (one edit)** | **~110 ms** |\n\nParsing, validating, compiling the execution plan, and serializing back to\nJSON are all effectively free — **under half a millisecond combined.** The\n110 ms is coming from somewhere else entirely. Breaking `commitNodeGraphPatch`\ndown into its actual sub-calls (same methodology) found it:\n\n| Sub-call inside `commitNodeGraphPatch` | Median time | Share |\n|---|---|---|\n| `applyNodeGraphPatchToDom` | **~57 ms** | ~50% |\n| `applyNodeGraphZoom` | **~21 ms** | ~19% |\n| `renderNodeGraphConnectionList` | **~11 ms** | ~10% |\n| `syncNodeGraphMonitorIndicators` | ~5 ms | ~5% |\n| everything else (validate, clone, runtime sync, palette, ghost sliders, filter curves, visual settings, settings view) | **< 1 ms total** | ~1% |\n\n**Finding: this was never a serialization problem.** It's a DOM-rebuild\nproblem — `applyNodeGraphPatchToDom` alone is ~500x slower than parsing the\nentire patch file. Any compression/binary-format work on the *file* would\nshave sub-millisecond savings off a 110 ms edit. The actual target for Phase 3\nis now clear: **why does applying a 7-node patch to the DOM cost 57 ms, and\ndoes it need to fully rebuild rather than diff against what's already\nrendered?**\n\n### 🩹 Phase 4, round 1: the heatmap was forcing layout on every edit\n\nBreaking `applyNodeGraphPatchToDom` itself down further (same monkey-patch-and-time\nmethodology, applied live in the running app) found two disproportionate costs\ninside it:\n\n| Sub-call inside `applyNodeGraphPatchToDom` | Median time |\n|---|---|\n| `applyNodeGraphWorkspaceView` | **~17 ms** |\n| `updateNodeGraphGridHeatmap` | **~13 ms** |\n| `renderNodeGraphCameraView` | ~7 ms |\n| everything else instrumented | < 2 ms |\n| *(remaining, the per-node update loop itself)* | ~39 ms |\n\nRoot cause for `updateNodeGraphGridHeatmap`: it reads `node.offsetWidth` /\n`node.offsetHeight` and `getComputedStyle(...)` for every visible node —\n**right after** the per-node loop above it just finished writing CSS custom\nproperties on those same nodes. That write-then-read sequence forces a\nsynchronous browser layout (a well-known pattern called \"layout thrashing\")\non every single patch commit, purely to redraw a cosmetic glow/mask overlay\nbehind the nodes.\n\nThe fix (`public/node-graph-patch-core.js`): swap the synchronous\n`updateNodeGraphGridHeatmap()` call for the already-existing\n`scheduleNodeGraphGridHeatmapUpdate()` — a one-line change that defers the\nheatmap repaint to the next animation frame instead of blocking the commit on\nit, exactly the same deferral pattern already used for wire redraws\n(`scheduleNodeGraphWireRedrawAfterLayout`) in the same function. No visual\nchange — the glow still updates, just one frame later, which is\nimperceptible for a cosmetic overlay.\n\n**Result, same measurement methodology, same patch:** median full\n`commitNodeGraphPatch` dropped from **~113.6 ms to ~91.4 ms** — about **20%\nfaster per edit**, from a single deferred call.\n\n`applyNodeGraphWorkspaceView`'s ~17 ms and the per-node loop's ~39 ms were\nleft untouched this round.\n\n### 🩹 Phase 4, round 2: a correct fix that turned out not to matter (reported honestly)\n\nDigging into `applyNodeGraphWorkspaceView`'s ~17 ms found it was **not**\n`clampNodeGraphWorkspaceGridSizeToViewport` as the round-1 writeup guessed —\nit was all inside `applyNodeGraphPan()`, which it calls. And `applyNodeGraphPan`\nturned out to have the *exact same* synchronous `updateNodeGraphGridHeatmap()`\ncall as round 1's fix, on a second, separate call path (it also runs on every\nlive pan/zoom drag, not just on patch commit).\n\nApplied the identical fix: swapped it for `scheduleNodeGraphGridHeatmapUpdate()`.\nSame reasoning, same zero-visual-risk deferral, same pattern as round 1.\n\n**Then measured it honestly instead of assuming it worked**, isolating repeated\n`applyNodeGraphPan()` calls the way live dragging would exercise them\n(`git stash` to get a true before/after on just this file): baseline median\n**~17 ms**, after the fix **~20 ms** — no improvement, within noise, if\nanything slightly worse. The reason: `nodeGraphRenderedOriginOffset()`, called\n*earlier* in the same function, already calls `getBoundingClientRect()` on the\nworkspace — so the forced layout was already paid before the heatmap read\never ran. Removing the heatmap's redundant read didn't remove the mandatory\nreflow, because it wasn't the one causing it.\n\n**The fix stayed in** — it's still strictly correct (one fewer redundant\nforced-layout trigger, no visual change, same win it already proved in the\n`commitNodeGraphPatch` path from round 1) — but it is **not** being credited\nwith a speedup it didn't produce in this scenario. The real remaining cost\nfor live pan/zoom is `nodeGraphRenderedOriginOffset`'s `getBoundingClientRect()`\ncall, which is the actual candidate for round 3.\n\n### 🩹 Phase 4, round 3: a real finding, and why it stops here for now\n\n`nodeGraphRenderedOriginOffset()` was calling `nodeGraphWorkspaceCenterOffset()`\nand `nodeGraphRenderedPan()` back to back — and both independently ran their\n*own* `getBoundingClientRect()` + `getComputedStyle()` on the same workspace\nelement, on the same tick, with no style write in between. That's a genuinely\nredundant duplicate measurement, so it was refactored into one shared read\n(`nodeGraphWorkspaceRectMetrics()`), passed into both — fully backward\ncompatible, every other caller of those two functions still works exactly as\nbefore.\n\n**Measured honestly again:** baseline ~17 ms, after the fix ~17.1 ms. No\nchange. The reason matters more than the fix: **a forced synchronous layout\nis paid once, by whichever read happens first after a style write — reading\nthe same geometry a second time right after is nearly free**, because the\nbrowser caches layout results until the next write invalidates them. Cutting\ntwo redundant reads down to one doesn't remove the cost, because the cost was\nnever about *how many* reads happen — it's about the fact that *any* read\nhappens at all, synchronously, on a tick that just wrote styles.\n\nThat's the real lever for a further win: **stop reading `getBoundingClientRect()`\non every pan/zoom tick at all**, by caching the workspace's rect and border\nmetrics across calls and only re-measuring when the workspace's actual\ngeometry changes (a resize). That's a bigger, riskier change — `ResizeObserver`\ncatches *size* changes but not pure *position* shifts (e.g. a sidebar\ntoggling, or the page scrolling), and this workspace rect feeds pixel-accurate\nmouse-to-graph coordinate math, so a stale cached position would show up as\nreal, hard-to-notice pan/zoom drift rather than a crash. Checking every place\nthe workspace can move without resizing turned out to have a cleaner\nalternative — see round 4.\n\n### 🩹 Phase 4, round 4: the real fix wasn't caching, it was throttling\n\nBefore attempting the risky rect-caching approach, one thing was worth\nchecking first: **how often does the code that reads this geometry actually\nrun during a real drag?** `dragNodeGraphWorkspacePan` is bound directly to raw\n`pointermove` events with no throttling — and modern pointer devices can fire\n`pointermove` far more than once per animation frame (a 240 Hz mouse can send\n4+ events inside a single 16 ms frame). Every one of those was calling\n`setNodeGraphPan` → `applyNodeGraphPan()` synchronously, forcing the same\nlayout read rounds 1–3 were built around, **multiple times per visible\nframe** — for positions that are never seen, since only the last one before\npaint actually gets drawn.\n\nThat's a narrower, safer fix than caching the workspace's geometry at all:\ncoalesce the pan-drag handler to **at most one applied position per animation\nframe**, using `requestAnimationFrame`. `preventDefault()`/`stopPropagation()`\nstay synchronous on every raw event (required to reliably block default\ntouch-scroll behavior) — only the expensive `setNodeGraphPan` call is\ndeferred and batched. On drag end, any pending frame is flushed immediately\nand synchronously, so the final position is always exact, never \"one frame\nstale.\"\n\nThis sidesteps the position-drift risk entirely — no geometry is cached\nacross time, so there's nothing to go stale from a sidebar toggle or page\nscroll. It only changes *how often* the existing, already-correct pan\ncalculation runs.\n\n**Verified, not assumed:** simulated a 40-event synchronous burst of\n`pointermove` calls (mimicking a high-poll-rate device sending many updates\nfaster than the browser can paint) — **zero** `applyNodeGraphPan` calls\nduring the burst (all deferred to the next frame), then **exactly one** call\non drag-end to flush, landing at the *exact* expected final pixel position\n(`x: 200, y: 150`, matching the last event precisely — no drift). For a\n40-events-per-frame drag, that's roughly a 40x reduction in forced-layout\nreads during that stretch, with zero precision loss.\n\nThis round is a clean stopping point for Phase 4: three of four rounds\ntargeted \"read less/read smarter\" and returned honest nulls once the\nduplicate/redundant reads were gone; round 4 found the actual lever was\n\"read less *often*,\" which is both the biggest win of the four and the\nlowest-risk to ship.\n\n---\n\n## 🧱 The layering this has to respect\n\n<div align=\"center\">\n<img src=\"/media/efficient-patch-system/patch-system-layering.svg\" alt=\"Five stacked layers — Human/Editor, Circuit/Runtime Graph, Binding (the bridge), DSP Memory, DSP Objects — with a Patch JSON load/save loop feeding back into the Runtime Graph layer\" width=\"70%\"/>\n</div>\n\nThis is the non-negotiable part, carried over directly from the direction set\nfor Soundemote's execution graph as a whole:\n\n```\nCircuit / runtime graph          <- humans see nodes, ports, wires, presets\n    -> node/parameter metadata\n    -> DSP binding metadata      <- the bridge, and only the bridge\n    -> externally owned DSP memory/state\n    -> low-level DSP object      <- plain, fast, SIMD-compatible, UI-blind\n```\n\nThe sacred invariant:\n\n> **Circuit does not own concrete DSP objects. DSP objects do not know Circuit.\n> Binding is the bridge.**\n\nPatch efficiency work has to happen *above* and *around* this boundary, not by\ncollapsing it. A faster patch loader that works by letting the graph reach\ninto DSP objects directly would be a faster way to build the wrong thing —\npointer soup, hidden ownership, and code that can never vectorize. The graph\ngets to be expressive and inspectable. The DSP layer underneath stays boring,\nexplicit, and contiguous, on purpose.\n\n---\n\n## 🪜 The proof ladder\n\nSpeed work here follows the same discipline as the binding work it sits on top\nof: small, sequential proofs, not a leap to a scheduler.\n\n```\nparameter sync\n    -> manual single-sample DSP chain\n    -> resynced manual DSP chain\n    -> manual block DSP chain\n    -> repeated block proofs\n    -> observe the common execution shape\n    -> only then design a scheduler\n```\n\nApplied to patch load/save/edit specifically, that ladder becomes:\n\n1. **Measure** — instrument today's load/save/edit path, get real numbers.\n2. **Minify, don't restructure** — cut incidental JSON overhead (whitespace,\n   redundant defaults) without changing the graph's data shape.\n3. **Speed the rebuild, not the format** — attack whatever the profiler\n   actually points at in the normalizer/execution-plan step.\n4. **Only then** consider whether the in-memory *shape* of a loaded patch\n   should change — and if it does, let that shape be pulled toward arrays of\n   parameters and stable IDs (SIMD-friendly), not toward a bespoke binary\n   blob invented in isolation.\n\nNo step here assumes the next one is needed. Each one only happens once the\nprevious one is proven.\n\n---\n\n## 🎛️ Where serialized connections meet SIMD and video\n\nTwo things on the horizon shape how \"efficient\" gets defined going forward,\neven though neither is being built yet:\n\n- **SIMD audio calculations.** The eventual DSP layer wants contiguous memory,\n  stable parameter slots, and fixed-size block processing — arrays, not\n  scattered per-node allocations. A patch format that already serializes\n  connections as stable IDs and flat parameter lists, rather than nested\n  object graphs, is one step closer to that memory shape without having\n  committed to it early.\n- **A work-in-progress video system.** If visual and audio signals eventually\n  share the same graph, the same serialized-connections format needs to\n  describe both without caring which one a given wire is. That argues for\n  keeping the patch format about *structure and bindings*, not about audio\n  specifically — the same reason `Circuit` isn't allowed to know about\n  concrete DSP objects today.\n\nNeither of these is a reason to build SIMD or video support now. They're a\nreason not to paint the patch format into a corner that would make either one\nharder later.\n\n### 🤝 The same shape also buys diff-friendly online collaboration — with a correction\n\nThere's a third reason to reach for stable IDs and flat structure, and it\nconverges on the exact same shape as SIMD instead of pulling in a different\ndirection:\n\n- **Arrays are diff-hostile *at the structural level*.** Today's\n  `nodes: [...]` are positional; keying them by stable ID (`{ [nodeId]: node }`)\n  turns identity from \"which array slot\" into \"which key\" — the same\n  stable-parameter-slot shape SIMD already wants.\n- **Structural edits and cosmetic/session state shouldn't merge as one unit.**\n  Keeping view/window state (`windows`, `view`, `cameras`) cleanly apart from\n  graph structure (`nodes`, `connections`, `modulations`) means a future\n  merge only has to reconcile the parts that are actually collaborative.\n\n**Correction, from round 1's actual testing:** the original version of this\nsection claimed keyed-by-id JSON \"turns a diff into one changed key\" as a\nplain diff/merge win. That claim was tested with real tools instead of\nassumed, and it does **not** hold for git's own tooling. Two experiments with\n`git merge-file` (a real 3-way text merge, not a guess):\n\n- **Non-overlapping concurrent edits** (one person edits an existing node's\n  param, another inserts a new node elsewhere): both the array shape and the\n  keyed shape merged cleanly, correctly, with **zero conflicts** — identical\n  outcome either way.\n- **A genuine conflict** (both people insert a *different* new node at the\n  *same* position): both shapes produced the **same conflict markers**,\n  because `git merge-file` diffs text lines, not JSON structure — it has no\n  concept of \"these are two different object keys that could coexist.\"\n\n**The honest conclusion:** for git-based diffing/merging specifically, this\nreshape buys **nothing measurable** — git's line-based `diff3` already\nhandles positional JSON arrays about as well as it handles keyed objects,\nfor both the clean and the conflicting case. The real value of keyed-by-id\nis narrower than originally claimed: it's a prerequisite for a *future,\npurpose-built, JSON-aware merge/collaboration engine* (one that operates on\nthe parsed object graph and can do a trivial key-union merge) — not a git\ndiff/merge win today. That collaboration-engine prerequisite is this fork's\nactual mandate (see Phase 6 below); SIMD-readiness is a separate agent's\nscope, not something this patch-system work is responsible for delivering —\nthis reshape simply tries not to obstruct it.\n\n---\n\n## 🔀 Phase 6: an actual multiplayer merge engine\n\nThis fork's mandate: the patch system, not SIMD. That includes making the\nsystem safe for multiple people editing the same patch at once — a real,\nscoped goal in its own right, separate from anything DSP/SIMD-related.\n\n**What's built (`public/node-graph-patch-lww-merge.js`):** a standalone,\npure last-writer-wins (LWW) merge engine, built and rigorously verified in\nisolation, before touching the live editor, `commitNodeGraphPatch`, or any\nnetworking — same \"prove the data path before the transport\" discipline as\neverywhere else in this plan.\n\n- Each node's position (`gx`/`gy`) and each `params.*` entry is tracked as\n  an independent field (`nodeId + fieldPath` as its key), stamped with an\n  `updatedAt` timestamp and a `siteId`. Two people editing *different*\n  fields on the same node both survive a merge — nothing gets clobbered.\n- Node existence is tracked separately via tombstones (`{ deleted, updatedAt,\n  siteId }`), so deletion has its own LWW resolution independent of field\n  edits — a stale field edit from before a delete doesn't resurrect the node.\n- Conflicting edits to the *same* field resolve deterministically: higher\n  timestamp wins; an exact-tie timestamp falls back to comparing `siteId`,\n  so the outcome never depends on which order the merge happens to run in.\n\n**Verified, not assumed — the actual CRDT correctness properties, tested\ndirectly:**\n\n| Property | What it guarantees | Verified |\n|---|---|---|\n| Commutative | `merge(A, B) === merge(B, A)` | ✅ |\n| Associative | `merge(merge(A,B), C) === merge(A, merge(B,C))` | ✅ |\n| Idempotent | `merge(A, A) === A` | ✅ |\n| Non-overlapping edits | Both survive, neither clobbers the other | ✅ |\n| Same-field conflict | Higher timestamp wins, deterministically | ✅ |\n| Delete vs. stale edit | Delete correctly wins over an earlier edit | ✅ |\n| Field edit after delete | Does not resurrect the node (tombstones govern existence) | ✅ |\n| Round trip through Phase 5's keyed format | Exact match on a real 7-node patch | ✅ |\n\nThese three properties (commutative/associative/idempotent) are what\nactually matter for a network merge engine: they're what guarantee every\ncollaborator converges on the identical result regardless of what order\nmessages arrive in, or whether one gets delivered twice. A merge function\nthat \"seems to work\" in a quick manual test but isn't provably commutative\nwill eventually let two clients silently disagree — that's why these were\ntested directly instead of just spot-checked.\n\n**What round 1 deliberately does NOT include, and why:**\n\n- **No networking/transport.** No websockets, no server-authority model, no\n  presence. Building that before the merge math is proven would be\n  inventing a scheduler before proving the data path — exactly the mistake\n  this whole plan has been structured to avoid.\n- **`paramMeta` isn't tracked.** Only user-edited state (position, param\n  values) goes through LWW; metadata isn't something two people \"edit\"\n  concurrently in a meaningful sense.\n- **Wall-clock timestamps, not a logical clock.** `updatedAt` as used in\n  testing is a plain number — real usage needs a clock-skew-resistant\n  source (a Lamport clock or server-assigned sequence number) before this\n  is safe across machines with different system clocks. Noted honestly as\n  an open gap, not silently assumed away.\n\n### 🔀 Phase 6, round 2: applying a merge to the live app, without crashing it\n\nRound 1 proved the merge *math*. It didn't prove the merge *result* could\nactually be applied to a running patch without breaking something — that's\na different question, and a real one: a merged doc that deletes a node is\neasy to build in the abstract, but the live app has connections that point\nat node ids by reference.\n\n**A real gap, found by reading the code instead of assuming it's handled:**\n`validateNodeGraphPatch` (`node-graph-patch-core.js`) *throws* — not\nsilently drops — when a connection references a node that no longer exists.\nSo applying a merged doc that deletes a connected node would crash the\nentire commit, not just lose the connection quietly. This is exactly the\nkind of bug that stays invisible until the first time someone actually\ndeletes a connected node during a merge.\n\n**The fix (`public/node-graph-patch-lww-live.js`, new):** a bridge function\nthat reconstructs a full patch from a merged doc, carries over everything\nthe LWW engine doesn't track (`paramMeta`, view/window/camera state — all\nuntouched from the currently open patch), and filters `connections` /\n`graphConnections` down to only the ones whose endpoints both survived the\nmerge, *before* handing the result to the same `commitNodeGraphPatch` every\nother edit in the app already goes through.\n\n**Verified against the real running app, not just pure data:**\n\n- Loaded an actual 7-node demo patch, simulated a remote collaborator\n  deleting a node that had 2 live connections. Before the fix's filtering\n  logic, this would throw. After: **no exception**, the node's exact 2\n  connections were dropped (5 → 3, matching precisely), and — checked\n  directly against the DOM, not just the in-memory patch — **the node's\n  actual rendered element was gone from the page.**\n- Two concurrent field edits on the *same* node (a remote param change, a\n  local position change) both landed correctly: verified in the in-memory\n  patch **and** by reading the live parameter slider's actual DOM value,\n  confirming the merge propagates all the way through\n  `applyNodeGraphPatchToDom` to what a user would actually see.\n\nThis closes the loop: pure merge (round 1) → correctly applying a merge\nresult to a live patch without crashing on stale references (round 2). The\nonly missing piece before this could support real multiplayer is a\ntransport to carry merged docs between two actual browser sessions.\n\n### 🔀 Phase 6, round 3: the actual network hop\n\n**The transport (`server.py` + `node-graph-patch-lww-transport.js`, new):**\nan in-memory, HTTP polling relay — deliberately not websockets. Two new\nendpoints: `POST /api/multiplayer/broadcast` (append a message to a\nsession's log) and `GET /api/multiplayer/poll?sessionId=…&since=…` (return\neverything appended since a given index). Session logs live only in\n`server.py`'s process memory, capped at 4,000 messages and 64 concurrent\nsessions, and are lost on restart — this is a proof that the pipeline works\nover a real network hop, not a production realtime transport. Polling\ninstead of websockets keeps the README's \"no package install needed\"\npromise true; this is genuinely the smallest thing that proves the point.\n\n**Verified against the real server, not simulated in-process:** ran an\nisolated `server.py` instance and hit it with real HTTP requests —\n\n- Broadcast a field edit and a delete as one client, polled them back as\n  another — both messages round-tripped exactly, in order.\n- Incremental polling (`since=1` after two broadcasts) returned *only* the\n  new message, not a resend of everything — the actual mechanism a live\n  client would use to avoid re-processing history every poll.\n- Input validation: a malicious/malformed `sessionId` (attempted injection\n  characters) and a non-object `message` body both correctly rejected with\n  `400`, not silently accepted or crashed on.\n\n**Then the full pipeline, end to end, in a real browser hitting a real\nserver:** one simulated client broadcast a param edit over the actual HTTP\nrelay this page was being served from; a second simulated client polled\nfor it, merged it into its own local doc, and applied the result to its\nlive patch — verified in the in-memory patch **and** the real DOM slider,\nboth showing the remote value. Broadcast → poll → merge → apply-to-DOM,\nover an actual network round trip, not a single in-process function call\npretending to be two clients.\n\n**What's still not here, on purpose:** no room/session-creation UI, no\npresence (who else is in this patch right now), no reconnect/resume logic.\nThose are each their own round if this is worth continuing — round 3's job\nwas proving the wire actually carries the message correctly, which it does.\n\n### 🔀 Phase 6, round 4: wiring it to real slider drags\n\nRound 4 closes the last item from round 3's \"not built\" list: dragging a\nparameter slider now actually broadcasts, and a background loop\nautomatically applies whatever comes back — no manual API calls required.\n\n**The hook point (`node-graph-slider-dragging.js`):**\n`syncNodeGraphPatchParameterFromSlider` is what actually writes a slider's\nvalue into the patch on every `input` event — and notably it does **not**\ncall `commitNodeGraphPatch` (that's Phase 4's expensive path; this function\nis the cheap one that runs on every drag tick). One line was added right\nafter it writes the value: call `nodeGraphLwwNotifyLocalFieldEdit`, which is\na no-op unless a multiplayer session is active. Off by default, and because\nit's gated at the top of that function, it adds no cost to the single-player\npath at all.\n\n**New file (`node-graph-patch-lww-editor-wiring.js`):** owns the session\nstate (`nodeGraphLwwMultiplayer`), a `startNodeGraphLwwMultiplayerSession(id)`\n/ `stopNodeGraphLwwMultiplayerSession()` pair, and a `setInterval`-driven\npoll loop (500ms) that merges incoming messages into a running local doc and\napplies them via round 2's live-apply bridge. No join/room UI exists — a\nsession is started by calling `startNodeGraphLwwMultiplayerSession` directly.\nThat control is still \"not built,\" same as before.\n\n**Verified, including a real bug caught in my own test methodology, not the\ncode:** the first attempt to verify the background timer — broadcast a\nremote edit, wait, check the value — *appeared* to fail across several\nseparate tool calls. Rather than assume the polling logic was broken,\nisolated it: ran the exact same broadcast-wait-check sequence as **one\natomic script** instead of spread across several. It passed immediately.\nThe real cause was the test's own methodology — the background timer keeps\nrunning between separate calls into the page, so checking state via\nmultiple round-trips raced against timer ticks whose exact timing I didn't\ncontrol. The atomic version removed that variable. Documenting this because\n\"the test looked like it failed\" and \"the code has a bug\" are different\nclaims, and conflating them would have led to debugging the wrong thing.\n\nWith that resolved, three things were confirmed directly:\n\n- **Zero behavior change when inactive:** dragged a slider with no session\n  started, intercepted `fetch` — confirmed no `/api/multiplayer/*` call was\n  made, and the value still wrote locally exactly as before this round.\n- **The real background timer, atomically verified:** started a session,\n  had a separate site broadcast a change, waited on the *actual*\n  `setInterval` (no manual poll call) — the value updated correctly in both\n  the in-memory patch and the live DOM slider.\n- **The real user-facing path:** dispatched a genuine DOM `input` event on\n  an actual slider element (not calling the broadcast function directly) —\n  confirmed the edit landed correctly on the server, exactly as another\n  client's poll would see it.\n\n**Still open:** node position (drag-to-move) and node create/delete aren't\nwired yet — only parameter sliders are. Timestamps are still plain\nwall-clock numbers (the clock-skew gap from round 1 is unresolved). No UI\nexists to start/stop a session or see who else is connected.\n\n### 🔀 Phase 6, round 5: a slider drag was broadcasting once per pixel\n\nA fair concern raised after round 4 shipped: does dragging a slider really\nbroadcast on *every pixel of movement*? Measured instead of assumed —\n**yes.** Simulating a fast drag (60 `input` events, roughly what a real drag\nproduces) resulted in **60 separate network broadcasts**, a strict 1:1\nratio, zero throttling. A slider drag can fire far more `input` events per\nsecond than a screen can even show, let alone than a network relay needs.\n\n**The fix:** the exact same rAF-coalescing technique Phase 4 round 4 already\nproved for pan-dragging — queue the latest value per field and flush at most\nonce per animation frame, instead of once per raw event. A user's hand can't\nproduce meaningfully distinct updates faster than the screen refreshes, so\nanything beyond one-per-frame was purely wasted network traffic.\n\n**A gap the first version of this fix had, caught by testing rather than\nassuming it worked:** relying purely on the next animation frame to flush\nhas a real failure mode — `requestAnimationFrame` can be suspended when a\ntab is backgrounded or not actively rendering. Measured directly: queued a\npending broadcast, waited 200ms of real wall-clock time, and it **never\nflushed** — the queued edit sat there indefinitely. Phase 4 round 4 already\nhad the answer to this from its own pan-drag work: don't rely purely on the\nnext frame — also flush explicitly at the natural \"end of this gesture\"\nsignal. For pan-dragging that was pointer-up; for a slider it's the native\n`change` event, which fires exactly once when the user releases it. Added a\n`change` listener (`node-graph-module-rendering.js`) that flushes\nimmediately, so the final value is guaranteed to go out promptly regardless\nof frame timing.\n\n**Verified, both halves:**\n\n- **Coalescing:** simulated 60 `input` events during a drag — **zero**\n  broadcasts while dragging, then exactly **one** on release, carrying the\n  correct final value. 60 events → 1 broadcast, not 60.\n- **Full round trip with the fix in place:** 30 drag events on a real\n  parameter, released — exactly one message landed on the server, with the\n  exact final value, and the live in-memory patch matched it precisely.\n\nThis directly answers the original concern: broadcast frequency is now\ncapped at once per animation frame per field (plus one guaranteed flush on\nrelease), not once per raw input event — the \"no faster than a human can\nactually perceive it\" ceiling the concern was asking for.\n\n---\n\n## 🗺️ Plan of attack\n\n| Phase | Goal | Status |\n|---|---|---|\n| 1 | Profile real load/save/edit timings on today's format | ✅ done — see numbers above |\n| 2 | Minify JSON output, measure the delta | ⏸️ deprioritized — Phase 1 showed parse/serialize is <1ms; not the bottleneck |\n| 3 | Identify the actual hot path in normalize/rebuild | ✅ done — it's DOM rebuild, not normalize/rebuild: `applyNodeGraphPatchToDom` (~50%), `applyNodeGraphZoom` (~19%), `renderNodeGraphConnectionList` (~10%) |\n| 4 | Targeted fix for the hot path, re-measure | ⏸️ paused after round 4 — see write-up. Round 1: deferred heatmap in commit path (~113.6ms → ~91.4ms, ~20% faster). Round 2 & 3: correct fixes, honestly measured **no** speedup (layout was already forced earlier / read count wasn't the cost). Round 4: rAF-throttled the pan-drag handler — verified ~40x fewer forced-layout reads during a fast drag, zero precision loss, no geometry caching/staleness risk |\n| 5 | Reshape patch data toward stable-ID-keyed collections | 🟡 round 1 done: `nodes` now serializes keyed by id (was a positional array). Backward compatible (old array-format saves still load), round-trip verified byte-identical. The diff-friendliness claim was tested and **corrected** — see write-up. `connections`/`graphConnections`/`modulations` not yet reshaped (need a composite key, separate round) |\n| 6 | Build a real multiplayer merge engine | 🟡 round 1: standalone LWW merge engine, verified directly. Round 2: bridged merged docs into the live app, found and fixed a crash risk. Round 3: an actual HTTP polling transport, verified end to end. Round 4: wired real slider drags to auto-broadcast + auto-apply. Round 5: found and fixed a real problem — drags were broadcasting once per pixel (60 events → 60 network calls); rAF-coalesced to once per frame, plus a guaranteed flush on release since `requestAnimationFrame` can stall in a backgrounded tab (caught by measuring, not assuming). Still missing: node position/create/delete wiring, presence/reconnect UI, a clock-skew-resistant timestamp source |\n\nThis table is the honest state of things: a plan, not a changelog. Phase 1's\nown numbers reordered the plan — they pointed straight past JSON format and\nstraight at DOM rebuild cost, so Phase 2 (minify) is parked and Phase 3\n(find the hot path) is already answered by the same measurement pass.\n\n---\n\n## 📄 License\n\nSource-available for noncommercial use only, same as upstream. Commercial use\nrequires a separate written commercial license from Soundemote. See\n[`LICENSE`](LICENSE).\n\n<div align=\"center\">\n\n*Measure first. Optimize second. Never the other way around.*\n\n</div>\n";
export const DIGITAL_EFFICIENT_PATCH_SYSTEM_REPO = "elanhickler/soemdsp-sandbox-digital-efficient-patch-system";

export const DIGITAL_SIGNALS_AUDIO_MARKDOWN = "<div align=\"center\">\n\n<img src=\"/media/white-wire/banner.svg\" alt=\"soemdsp-sandbox — Digital Signals\" width=\"100%\"/>\n\n# 🔌 soemdsp-sandbox — Digital Signals\n\n*A fork of [soemdsp-sandbox](https://github.com/soundemote/soemdsp-sandbox) exploring one idea:*\n**every wire in this patcher already carries a single JavaScript float — what if some of them carried *bits* instead of a voltage?**\n\n[![License: Noncommercial](https://img.shields.io/badge/license-noncommercial-blue.svg)](LICENSE)\n[![Language: C++/WASM](https://img.shields.io/badge/native-C%2B%2B%20%E2%86%92%20WASM-654ff0.svg)](native_modules)\n[![Runtime: Vanilla JS](https://img.shields.io/badge/runtime-vanilla%20JS-f7df1e.svg)](public)\n[![Status: Experimental](https://img.shields.io/badge/status-experimental-orange.svg)](#)\n[![Made with ❤ and bits](https://img.shields.io/badge/made%20with-%E2%9D%A4%20and%20bits-white.svg)](#)\n\n</div>\n\n<div align=\"center\">\n\n## 🖤 `beauty = binary * chaos`\n\n<img src=\"/media/white-wire/beauty-equals-binary-times-chaos.png\" alt=\"A dense tangle of trajectories filling a square, in matched inverted black-on-white and white-on-black panels\" width=\"80%\"/>\n\n*Two renderings of the same signal, one negative of the other — order and its inverse,*\n*both true at once. That's the whole thesis of this repo in one image.*\n\n📌 Original post: [bsky.app/profile/soundemote.bsky.social/post/3mk3zh2zwyk2p](https://bsky.app/profile/soundemote.bsky.social/post/3mk3zh2zwyk2p)\n\n</div>\n\n---\n\n## 📖 Contents\n\n- [`beauty = binary * chaos`](#-beauty--binary--chaos)\n- [What's a \"digital signal\"?](#-whats-a-digital-signal-here)\n- [Lossy by design](#-lossy-by-design)\n- [What's built here](#-whats-built-here)\n- [Where this can go](#-where-this-can-go)\n- [Running it](#-running-it)\n- [License](#-license)\n\n---\n\n## 💡 What's a \"digital signal\" here?\n\nZero and one are the two smallest true things in the world, and everything in this\nrepo is what happens when you take that seriously.\n\nEvery existing wire in the sandbox is **analog** by convention — a continuous control\nvoltage or audio signal riding on a single float. A **digital signal** is that *same*\nfloat, loved differently: as a fixed-width integer whose individual bits each mean\nsomething. Twelve booleans (*is C held? is C♯ held? …*) packed into one number, riding\none wire, instead of twelve separate gate wires. One float, secretly a small constellation\nof on/off truths.\n\n> This isn't a hack bolted onto the CV system — it's a second signal type that was\n> always possible, hiding inside the same \"one wire, one float\" mechanism the whole\n> graph already runs on. The bits were always there. We just started asking the wire\n> what it actually knew.\n\n<div align=\"center\">\n<img src=\"/media/white-wire/wire-diagram.svg\" alt=\"Analog wire vs. digital signal wire, both riding a single float\" width=\"85%\"/>\n</div>\n\n### 🧮 The math behind it\n\nA JS double safely represents integers up to `2^53 − 1` before precision loss\n(`Number.MAX_SAFE_INTEGER`).\n\n| Quantity | Value |\n|---|---|\n| Safe bits on one wire | **53** |\n| Possible states | **9,007,199,254,740,992** |\n| Bits used today (pitch-class mask) | 12 |\n| Bits left on the table | **41** |\n\nDigital signals get a **white wire** in the graph — visually distinct from the usual\nrole-colored analog wires (🔵 cyan input, 🟠 amber output, 🟣 purple modulation).\nColors only — no change to wire shape, dashing, or animation.\n\n---\n\n## ♻️ Lossy by design\n\nThe most useful digital signal built so far — the 12-bit pitch-class mask — is\ndeliberately **lossy**: it throws away *which octave* a note came from and keeps only\n*which pitch class*. That's not a limitation, it's the point. Binary doesn't need to\nremember everything to be beautiful — it needs to remember the *right* things, and let\nthe rest go.\n\n> A signal that keeps exactly the bits a listener needs, and discards the rest, is\n> more useful than one that insists on exact reconstruction.\n\nAnd the inverse is just as true, which is the whole reason the image above exists:\nthe same tangle of order, rendered white-on-black and black-on-white, is still the same\ntangle. A bit doesn't care which of its two states you call \"on.\" Chaos and structure\nare two photographs of the same signal, and binary is what lets you develop either one.\n\n---\n\n## 🧩 What's built here\n\n| Module | What it does |\n|---|---|\n| 🎛️ **Turing Machine** | Classic mutating shift-register sequencer. Each clock edge shifts its register and randomly flips the new bit at a set probability, producing evolving, semi-repeating loops. Exposes its low 12 bits as a `Scale` output. |\n| 🎹 **Pitch Quantizer** | Snaps a `0.1V/Oct` pitch signal to the nearest note in a scale. Pick a preset (Chromatic, Major, Minor, Major/Minor Pentatonic, Whole Tone) or feed it a `Scale` mask directly — same bit convention, no adapter needed. |\n| 🎼 **Chord Sequencer** | Steps through one of six built-in diatonic progressions (I–V–vi–IV, ii–V–I, …) on each clock edge. Outputs the current chord as a `Scale` mask, its root as `Root` (`0.1V/Oct`), and a `Gate`. Native C++/WASM. |\n| 🧠 **Chord Memory** | Latches up to four notes from a single mono pitch source, one at a time, via a `Latch` trigger. Outputs them stacked or arpeggiated — built to sidestep needing real keyboard polyphony, which this sandbox doesn't have yet. |\n| ⚡ **`0.1V/Oct`** | Not a bitmask, but the other sense of \"digital\": a fixed, quantized representation (`semitone = value × 120`) rather than a free-form analog voltage. Every `0.1V/Oct` port in the sandbox gets the same white wire. |\n| 🌀 **Henon Map · Chua Attractor · Logistic Map** | The chaos generators that led here. Native WASM with a JS fallback, each verified against its real compiled artifact (randomized sweeps, bounded-output checks, exact iteration-count matching) before being wired into the graph. |\n| 🔲 **LUT Cell** | A real FPGA logic slice, modeled directly: a 4-input lookup table (`A`/`B`/`C`/`D`) feeding a clocked D flip-flop. `Truth Table` is itself a 16-bit digital signal — bit *i* is the cell's output for input combination *i*. `Out` is the instant combinational result, `Q` only updates on a `Clock` rising edge. Defaults to the 4-input XOR/parity table (`0x6996`). Leave `Clock`/`A` unwired and it free-runs at 220 Hz so a bare cell does something the moment you drop it in — wire either one for real and it behaves as a strict, silent-until-driven logic cell instead. |\n\n**Quick melodic patch:** `Chord Sequencer.Scale` → `Pitch Quantizer.Scale`, and\n`Chord Sequencer.Root` → `Pitch Quantizer.0.1V/Oct`. Clock it, pick a progression,\nand you've got a chord-anchored melody scaffold in two wires.\n\n---\n\n## 🚀 Where this can go\n\nA digital signal is just bits, which means corrupting it on purpose is just math —\nfiltering, mangling, and glitching become **first-class operations** instead of edge\ncases:\n\n- 💥 **Bit Crusher** — AND against a random/shrinking mask; notes vanish and reappear unpredictably.\n- 🦠 **Bit Rot** — a standalone corruption module for *any* digital signal: each clock tick, a chance to permanently flip one more bit.\n- ⚔️ **XOR Collision** — combine two digital signals; get exactly the bits present in one but not the other.\n- 🪞 **Bit Reverse** — mirror the bit order for an instant, unrelated-feeling variation on the same signal.\n- 💣 **Popcount Detonator** — count the set bits; cross a threshold, fire a trigger. Chaos with a consequence.\n\nNone of this works on a continuous CV wire. It only exists because the signal is\ndiscrete bits you're allowed to mangle.\n\n> 🥁 **Coming later:** a rhythm-generator counterpart sending multichannel timing\n> (a 4- or 8-track impulse), plus a step-sequencer counterpart, both speaking the\n> same digital-signal convention.\n\n---\n\n## 📄 License\n\nSource-available for noncommercial use only, same as upstream. Commercial use\nrequires a separate written commercial license from Soundemote. See\n[`LICENSE`](LICENSE).\n\n<div align=\"center\">\n\n**`beauty = binary * chaos`**\n\n*Built one bit at a time. 🔧*\n\n</div>\n";
export const DIGITAL_SIGNALS_AUDIO_REPO = "elanhickler/soemdsp-sandbox-digital-signals-audio";


export const RHYTHM_AND_PITCH_GENERATOR_MARKDOWN = "<div align=\"center\">\n\n<img src=\"/media/white-wire/banner.svg\" alt=\"soemdsp-sandbox - Rhythm and Pitch Generator\" width=\"100%\"/>\n\n# Soundemote Rhythm and Pitch Generator\n\nAn archaeology-and-rebuild fork for bringing the old Soundemote Rhythm and Pitch\nGenerator idea into `soemdsp-sandbox` as modern native C++/WASM modules.\n\n[![License: Noncommercial](https://img.shields.io/badge/license-noncommercial-blue.svg)](LICENSE)\n[![Runtime: soemdsp sandbox](https://img.shields.io/badge/runtime-soemdsp%20sandbox-63d2ff.svg)](public)\n[![Native Modules: C++ to WASM](https://img.shields.io/badge/native-C%2B%2B%20to%20WASM-654ff0.svg)](native_modules)\n[![Focus: rhythm + pitch](https://img.shields.io/badge/focus-rhythm%20%2B%20pitch-f7c948.svg)](#what-this-is)\n\n[Watch the Rhythm and Pitch Generator video](https://www.youtube.com/watch?v=lq7ilJj69u8)\n\n[![Rhythm and Pitch Generator video](https://img.youtube.com/vi/lq7ilJj69u8/maxresdefault.jpg)](https://www.youtube.com/watch?v=lq7ilJj69u8)\n\n</div>\n\n---\n\n## What This Is\n\nThis repository is a focused fork of `soemdsp-sandbox` for rebuilding the old\nSoundemote Rhythm and Pitch Generator concept. The goal is not simply to copy an\nold plugin. The goal is to extract the useful musical idea from the old\nSE/JUCE-era code and re-express it in the sandbox's current language:\n\n- small native C++/WASM DSP modules,\n- explicit module metadata,\n- graph patching,\n- visible oscilloscope/readout feedback,\n- and shareable web patches.\n\nThe old Rhythm and Pitch Generator was a self-contained generative instrument:\na master clock, rhythm selector, pitch selector, envelope, oscillator, and UI\nskin all bundled together. In the sandbox, those same ideas can become smaller\npatchable pieces.\n\n---\n\n## Source Lineage\n\nThe reference code was imported from:\n\n```text\nC:\\Users\\argit\\Documents\\_PROGRAMMING\\oldcode\\old stuff se_framework\\Modules\\RaPG\n```\n\nIt is now vendored into this repository at\n[`legacy/RaPG`](legacy/RaPG). That folder is an archive/reference drop, not a\nbuild target.\n\nImportant files:\n\n| File | Role |\n|---|---|\n| `RhythmAndPitchGeneratorCore.h` | Core timing, rhythm, pitch, oscillator, clock-divider, and note-selection classes. |\n| `RhythmAndPitchGeneratorCore.cpp` | Runtime behavior for rhythm clocks and clock-trigger selection. |\n| `RhythmAndPitchGeneratorModule.h` | Plugin/module shell and child module declarations. |\n| `RhythmAndPitchGeneratorModule.cpp` | Parameter definitions, callback wiring, child-module setup, and sample processing. |\n| `RhythmAndPitchGeneratorEditor.cpp` | Old UI layout, labels, help text, controls, and visualizers. |\n| `RhythmAndPitchGeneratorEditor.h` | Editor widget declarations. |\n| `Graphics.h/.cpp` | Old image-button, preset, and UI-helper widgets. |\n| `images/` | Background, clock lights, enable buttons, arrows, knob SVG, and preset section art. |\n\nThe old UI assets have been copied into this repo under\n[`docs/assets/rapg`](docs/assets/rapg) so the README and future docs can refer\nto them without depending on a private local folder.\n\n<div align=\"center\">\n  <img src=\"/media/rhythmandpitchgenerator/RPGENERATOR-BACKGROUND.png\" alt=\"Old Rhythm and Pitch Generator background art\" width=\"80%\"/>\n  <br/>\n  <em>Original Rhythm and Pitch Generator background asset.</em>\n</div>\n\n<br/>\n\n<div align=\"center\">\n  <img src=\"/media/rhythmandpitchgenerator/btn_light_off.png\" alt=\"Light button off\" height=\"36\"/>\n  <img src=\"/media/rhythmandpitchgenerator/btn_light_on.png\" alt=\"Light button on\" height=\"36\"/>\n  <img src=\"/media/rhythmandpitchgenerator/btn_cdivider_off.png\" alt=\"Clock divider off\" height=\"36\"/>\n  <img src=\"/media/rhythmandpitchgenerator/btn_cdivider_on.png\" alt=\"Clock divider on\" height=\"36\"/>\n  <img src=\"/media/rhythmandpitchgenerator/clock_light_off.png\" alt=\"Clock light off\" height=\"36\"/>\n  <img src=\"/media/rhythmandpitchgenerator/clock_light_on.png\" alt=\"Clock light on\" height=\"36\"/>\n</div>\n\n---\n\n## Code Detail\n\n### Top-Level Module\n\n`RhythmAndPitchGeneratorModule` is the old instrument container. It owns:\n\n- `RaPG_EnvelopeModule`\n- `RaPG_OscillatorModule`\n- `RaPG_PitchModule`\n- `RaPG_RhythmModule`\n- a `MidiMaster`\n- a smoothing manager\n- modulation sources\n- master/reset timers\n- oversampling and anti-alias filters\n\nThe old processing loop is a classic bundled-instrument loop:\n\n1. update smoothing and modulation,\n2. advance master and reset timers,\n3. run rhythm selection,\n4. run pitch selection,\n5. read the envelope,\n6. modulate oscillator amplitude/filtering,\n7. generate mono output,\n8. apply master volume, DC filtering, and clipping.\n\nKey top-level parameters:\n\n| Parameter | Range | Default | Meaning |\n|---|---:|---:|---|\n| `division` | `0..27` | `21` | Master tempo division, mapped through `indexToTimeSigValue`. |\n| `globalResetBars` | `0.001..64` | `2` | Bar interval for resetting rhythm and pitch. |\n| `masterRate` | `0.01..500` | `1` | Global time multiplier. Above `128`, old code injects random rate instability. |\n| `pitchMod` | `0..1` | `1` | Lets `masterRate` affect oscillator pitch. |\n| `masterVolume` | `0..2` | `1` | Final output gain. |\n| `paramSmooth` | `0.001..1` | `0.01` | Smoothing time for interactive/host parameter changes. |\n| `oversample` | `1..8` | `2` | Old anti-alias quality/CPU tradeoff. |\n\n### Rhythm Engine\n\nThe rhythm side is built around `RaPG_RhythmGenerator` and seven child\n`RaPG_ClockModule` instances.\n\nAt each master-clock trigger:\n\n1. the generator optionally wraps `currentStep` by `steps`,\n2. resets its noise at step zero,\n3. samples a seeded noise value,\n4. maps that value across the enabled clock dividers,\n5. selects a current clock and a ghost clock,\n6. triggers all clock dividers,\n7. outputs the selected clock index as a normalized value.\n\nEach clock divider has:\n\n- `divide`\n- `pulse1time`\n- `pulse2time`\n- `enable`\n\nThe two pulse times are normalized delays inside the clock division. This is one\nof the most interesting parts of the old design: a single selected clock can\nproduce a primary and secondary click, giving the rhythm generator a built-in\ndouble-hit/ghost-hit feel.\n\nRhythm parameters:\n\n| Parameter | Range | Default | Meaning |\n|---|---:|---:|---|\n| `enable` | `0..1` | `1` | Enables/disables stochastic rhythm selection. |\n| `seed` | `0..999` | `0` | Seed for the rhythm noise generator. |\n| `steps` | `0..128` | `0` | Step loop length; zero effectively disables step wrapping. |\n| `divide` | `1..128` | `4` | Master clock division for rhythm selection. |\n| `manual` | `-7..7` | `0` | Offset into the selected enabled clock index. |\n| `enableGhostClicks` | `0..1` | `0` | Enables the ghost-clock path. |\n\n### Pitch Engine\n\n`RaPG_PitchQuantizer` is a held-note selector. It tracks MIDI notes, samples a\nseeded noise value on its clock, and chooses one of the held notes, optionally\noffsetting across octaves.\n\nAt each pitch-clock trigger:\n\n1. it optionally wraps `currentStep` by `numSteps`,\n2. resets pitch noise at step zero,\n3. samples a new noise value,\n4. maps that value to a note index across currently held notes,\n5. wraps the index across octaves,\n6. calls either `newNoteFunction` or `legatoNoteFunction`,\n7. starts a note-length timer,\n8. later calls `noteOffFunction`.\n\nPitch parameters:\n\n| Parameter | Range | Default | Meaning |\n|---|---:|---:|---|\n| `enable` | `0..1` | `1` | Enables stochastic pitch selection. |\n| `seed` | `0..999` | `0` | Seed for the pitch noise generator. |\n| `steps` | `0..128` | `0` | Step loop length for pitch noise reset. |\n| `divide` | `1..128` | `4` | Clock division for pitch changes. |\n| `noteLengthMod` | `0..2` | `1` | Note duration as a fraction/multiple of the pitch clock. |\n| `octaveAmp` | `0..8` | `1` | Amount of octave movement produced by the noise value. |\n| `octaveOffset` | `-8..8` | `0` | Octave bias/offset. |\n| `bipolar` | `0..1` | `0` | Switches pitch noise mapping from unipolar to bipolar. |\n\n### Envelope Engine\n\nThe old envelope is a curve-shaped ADSR-like modulator. It can be triggered by:\n\n- user note,\n- rhythm click,\n- master reset,\n- ghost rhythm click.\n\nEnvelope parameters:\n\n| Parameter | Range | Default | Meaning |\n|---|---:|---:|---|\n| `attack` | `0.001..10` | `0.003` | Attack time. |\n| `decay` | `0.001..10` | `0.25` | Decay time. |\n| `sustain` | `0..1` | `0` | Sustain level. |\n| `release` | `0.001..10` | `0.1` | Release time. |\n| `attackCurve` | `-10..10` | `1` | Attack curve shape. |\n| `decayCurve` | `-10..10` | `1` | Decay curve shape. |\n| `releaseCurve` | `-10..10` | `0` | Release curve shape. |\n| `velocityMod` | `0..1` | `1/3` | Ghost-click amplitude relationship. |\n\n### Oscillator Engine\n\n`RaPG_Oscillator` extends `TriSawOscillator`, then adds:\n\n- pitch smoothing,\n- pitch/frequency modulation,\n- amplitude modulation,\n- one-pole lowpass filtering,\n- high-frequency pitch instability/noise,\n- master-rate-aware glide timing.\n\nOscillator parameters:\n\n| Parameter | Range | Default | Meaning |\n|---|---:|---:|---|\n| `phase` | `-1..1` | `0` | Phase offset. |\n| `frequency` | `-3000..3000` | `0` | Frequency offset/modulation. |\n| `pitch` | `-72..72` | `0` | Pitch offset in semitone-ish units. |\n| `amplitude` | `0..1` | `0.5` | Oscillator amplitude. |\n| `saw` | `-1..1` | `0` | Asymmetry / saw shaping. |\n| `spike` | `-1..1` | `0` | Symmetric bend / spike shaping. |\n| `square` | `-1..1` | `0` | Square-like shaping. |\n| `sine` | `-1..1` | `1` | Sine-like shaping. |\n| `knee` | `-1..1` | `0` | Knee/transition shaping. |\n| `lpCut` | `0.1..2200` | `2200` | Lowpass cutoff control. |\n| `ampFilterEnvMod` | `-1..1` | `0` | Envelope routing between amplitude and filter. |\n| `glideSpeed` | `0.0001..2` | `0.0001` | Time-scaled pitch glide. |\n\n### Internal Modulation Sources\n\nThe old code registers several values as modulation sources:\n\n| Source | Meaning |\n|---|---|\n| `oscSource` | Raw oscillator shape output. |\n| `oscFiltered` | Filtered oscillator output. |\n| `oscFltAmp` | Filtered and amplitude-modulated oscillator output. |\n| `oscFinalOut` | Final oscillator output value. |\n| `envSource` | Envelope output. |\n| `pitchNoiseSource` | Pitch selector noise value. |\n| `rhythmClockIndex` | Selected rhythm clock index, normalized. |\n| `rhythmNoiseSource` | Rhythm selector noise value. |\n| `rhythmClickSource` | Ghost-click amplitude value. |\n\nThis is one of the most important bridges into the modern sandbox. These are\nbasically display/control signals before the current sandbox had explicit\ndisplay-signal language.\n\n---\n\n## How This Should Become Sandbox Modules\n\nThe old generator was one big instrument. The sandbox version should become a\nsmall family of modules:\n\n| Sandbox module idea | Old-code source |\n|---|---|\n| Rhythm Generator | `RaPG_RhythmGenerator`, seven `RaPG_ClockDivider`s, rhythm seed/steps/manual/ghost logic. |\n| Pitch Generator | `RaPG_PitchQuantizer`, held-note tracking, seeded pitch noise, octave mapping. |\n| RaPG Envelope | `RaPG_EnvelopeModule`, curve ADSR, trigger/reset modes. |\n| RaPG Oscillator | `RaPG_Oscillator`, TriSaw shaping, filter, glide, envelope modulation. |\n| Clock Divider / Dual Pulse | `RaPG_ClockModule`, `pulse1time`, `pulse2time`, enable state. |\n\nThe first stable target should be smaller than the whole old instrument:\n\n1. build Rhythm Generator as a native C++/WASM source,\n2. output simple trigger/gate signals first,\n3. add selected-clock index and ghost-click outputs,\n4. build Pitch Generator as a separate native C++/WASM source,\n5. wire Rhythm Generator -> Pitch Generator -> oscillator in a normal sandbox\n   patch,\n6. only then consider rebuilding the full old combined instrument.\n\n---\n\n## Visual Direction\n\nThe old UI was compact, custom, and physical: clock lights, little image\nbuttons, a preset strip, a dark background, and helper text attached to\ncontrols. The modern sandbox should not clone that UI exactly, but the visual\nlanguage is useful:\n\n- rhythm should visibly feel like clocks and lights,\n- pitch should visibly feel like note choice and octave movement,\n- ghost clicks should be visible, not hidden,\n- seeded random should look intentional and repeatable,\n- reset/step behavior should be readable.\n\nThe old assets in [`docs/assets/rapg`](docs/assets/rapg) are here as reference\nmaterial, not as a requirement to recreate the old skin pixel-for-pixel.\n\n---\n\n## Current Status\n\nThis repo is a staging fork for the Rhythm and Pitch Generator rebuild. The\nREADME now captures the old code structure and the intended porting direction.\nImplementation should proceed module by module, with each native module\nverified in isolation before it becomes part of the default patch library.\n\n---\n\n## License\n\nSource-available for noncommercial use only, same as upstream. Commercial use\nrequires a separate written commercial license from Soundemote. See\n[`LICENSE`](LICENSE).\n";
export const RHYTHM_AND_PITCH_GENERATOR_REPO = "elanhickler/soemdsp-sandbox-rhythmandpitchgenerator";

export const VACTROLS_MARKDOWN = "# 🔦 Vactrols — a light-physics field guide\n\n> *A vactrol is two components that have never touched, whispering to each*\n> *other in light instead of wire.*\n\nThis is a fork of [`soemdsp-sandbox`](https://github.com/soundemote/soemdsp-sandbox)\nthat gives its **Vactrol Envelope** modules (🐢 `VTL5C3` and 🐌 `VTL5C4`) real\nlight-physics grounding: what a vactrol physically is, why it sounds the way\nit does, how that maps onto the module's knobs, and where the numbers came\nfrom.\n\n## 🔊 Listen: real-hardware vactrol recordings\n\nThree takes on the same real vactrol under CV control — the fast turn-on /\nslow, lingering release you hear is exactly the asymmetric photoconductive\npersistence described below. Not a filter sweep, not an envelope generator —\njust a photoresistor genuinely taking its time to let go of the light it\nsaw. 🎧\n\n- 🎛️ **[`vactrol_speed_test(cv).wav`](https://drive.google.com/file/d/1pIIddKLbYH8M8E1G6PcH-F1zifknBiIn/view?usp=drive_link)**\n  — driven directly by CV, the baseline response.\n- 🐌 **[`vactrol_speed_test(slow).wav`](https://drive.google.com/file/d/1q3_TV43KSkOH8tFBpTcoBHg0cgvoh4CY/view?usp=drive_link)**\n  — the same cell pushed toward its slow, swelling release.\n- 🐢 **[`vactrol_speed_test(fast).wav`](https://drive.google.com/file/d/11xbIYxlobjik55lM_NmLfSVu7C5XClt2/view?usp=drive_link)**\n  — and toward its fast, plucky release, for comparison.\n\nAnd two demos of the **Buchla-style resonant low-pass gate** ([why LPGs love\nvactrols](#️-why-lpgs-love-vactrols)) in its two modes — bandpass and\nlowpass — showing the same vactrol simultaneously shaping amplitude and\ntimbre from one control signal:\n\n- 🎚️ **[`buchla_badpass.flac`](https://drive.google.com/file/d/1ZNMxsp3G-EnbpTmFFS3RJ9u1PjOVMTsJ/view?usp=drive_link)**\n  — Buchla-style bandpass LPG mode.\n- 🎚️ **[`Buchla-LoPass.flac`](https://drive.google.com/file/d/1ae_8SFkIKIuHRL0LX35xUoCySTBnDLQQ/view?usp=drive_link)**\n  — Buchla-style lowpass LPG mode.\n\n> ⚖️ \"Buchla\" is used here purely descriptively, to name a well-known circuit\n> topology (the resonant low-pass gate) associated with that historic design\n> style — not as a claim of association. This project and these recordings\n> are **not affiliated with, endorsed by, or sponsored by Buchla Electronic\n> Musical Instruments** or any successor entity.\n\n---\n\n## 📖 Table of contents\n\n- [🔊 Listen: real-hardware vactrol recordings](#-listen-real-hardware-vactrol-recordings)\n- [🕯️ What is a vactrol?](#️-what-is-a-vactrol)\n- [🧪 The physics in one paragraph](#-the-physics-in-one-paragraph)\n- [📐 Anatomy](#-anatomy)\n- [⏱️ Fast vs. slow: VTL5C3 vs VTL5C4](#️-fast-vs-slow-vtl5c3-vs-vtl5c4)\n- [📊 Full VTL5C-series comparison table](#-full-vtl5c-series-comparison-table)\n- [🎛️ How this maps to the sandbox module](#️-how-this-maps-to-the-sandbox-module)\n- [🧮 The DSP model](#-the-dsp-model)\n- [🎚️ Why LPGs love vactrols](#️-why-lpgs-love-vactrols)\n- [📚 Scientific papers & primary sources](#-scientific-papers--primary-sources)\n- [💬 Community references](#-community-references)\n- [⚖️ A note on naming & IP](#️-a-note-on-naming--ip)\n\n---\n\n## 🕯️ What is a vactrol?\n\nA **vactrol** (portmanteau of *vacuum* + *photoresistor*, though there's no\nvacuum involved — the name just stuck) is an **opto-isolator built from an\nLED (or incandescent lamp) optically coupled to a CdS photoresistor (LDR)**,\nsealed together inside a light-tight tube with **zero electrical connection**\nbetween the two halves. 🚫🔌\n\nYou drive the LED with a control-voltage-derived current. The LED glows. The\nphotocell \"sees\" that light and its resistance drops. Nothing about that\nresistance change is instantaneous or linear — and that imperfection is\n*exactly* why synth designers love it.\n\n## 🧪 The physics in one paragraph\n\nCdS (cadmium sulfide) photoresistors follow a **power-law relationship**\nbetween illuminance and resistance:\n\n```\nR(lux) ∝ Lux^(−γ)\n```\n\n...where `γ` (the *photoconductive gamma*) typically sits around **0.7–0.9**\nfor real cells. But the *really* musically useful part is the **asymmetric\ntime response**: charge carriers in the CdS crystal get freed almost\ninstantly when light hits (fast **attack**), but *linger*, trapped in\ncrystal defect states, for a comparatively long time after the light is gone\n(slow **release**). This is a real solid-state phenomenon called\n**photoconductive persistence** — it is not a capacitor, there is no RC\nnetwork, and yet it behaves like a smoothing filter purely because of how\ncharge carriers de-trap over time. ⚡➡️🐌\n\n## 📐 Anatomy\n\n![Vactrol anatomy diagram](/media/vactrols/vactrol-anatomy.svg)\n\nTwo components, one tube, one beam of light between them. That's the whole\ncircuit. The complete lack of electrical coupling is also why vactrols are\nprized for **galvanic isolation** in non-audio contexts (relays, medical\nequipment) — the audio/synth use is almost a happy side effect of a\ncomponent designed for something else entirely. 🧰\n\n## ⏱️ Fast vs. slow: VTL5C3 vs VTL5C4\n\n![VTL5C3 vs VTL5C4 release curves](/media/vactrols/vactrol-release-curves.svg)\n\n| | 🐢 VTL5C3 | 🐌 VTL5C4 |\n|---|---|---|\n| **Character** | Fast, percussive, \"plucky\" | Slow, swelling, \"breathing\" |\n| **Turn-on (attack, 63%)** | ~2.5 ms | ~6.0 ms |\n| **Turn-off (release, to 100 kΩ)** | ~35 ms | **~1.5 s** (≈40× slower) |\n| **Dark resistance (min)** | 10 MΩ | 400 kΩ |\n| **Typical use** | Buchla/Serge-style resonant LPG \"pop\" | Long pads, compressor-like glide |\n| **Synth-DIY reputation** | *The* standard LPG vactrol | *The* slow alternative |\n\nThe eye-catching number is the release time: **VTL5C4 takes roughly 40×\nlonger than VTL5C3 to let go of a sound.** Same physical mechanism, same\npackage, wildly different musical personality — just because the CdS cell\ninside was doped/processed differently. 🎨\n\n## 📊 Full VTL5C-series comparison table\n\nAll figures below are drawn from the official **PerkinElmer Optoelectronics**\ndatasheets for the VTL5C-series axial vactrols (see\n[Sources](#-scientific-papers--primary-sources)). \"Turn-on\" is time to 63% of\nfinal `R_ON` after a 40 mA drive pulse; \"turn-off\" is time to reach 100 kΩ\n(or as noted) after the pulse ends.\n\n| Part | R_ON @1/10/40 mA | R_OFF (dark, min) | Dynamic range | Turn-on (63%) | Turn-off |\n|---|---|---|---|---|---|\n| VTL5C1 | 20 kΩ / 600 Ω / 200 Ω | 50 MΩ | 100 dB | 2.5 ms | 35 ms |\n| VTL5C2 | 5.5 kΩ / 800 Ω / 200 Ω | 1 MΩ | 69 dB | 3.5 ms | 500 ms |\n| **VTL5C3** | 30 kΩ / 5 Ω / 1.5 Ω | 10 MΩ | 75 dB | **2.5 ms** | **35 ms** |\n| **VTL5C4** | 1.2 kΩ / 125 Ω / 75 Ω | 400 kΩ | 72 dB | **6.0 ms** | **1.5 s** |\n| VTL5C6 | 75 kΩ / 10 kΩ / 2 kΩ | 100 MΩ | 88 dB | 3.5 ms | 50 ms (→1 MΩ) |\n| VTL5C7 | 5 kΩ@0.4mA / 1.1 kΩ@2mA | 1 MΩ | 75 dB | 6 ms | 1 s (→100 kΩ) |\n| VTL5C8 | 4.8 kΩ / 1.8 kΩ / 1 kΩ | 10 MΩ | 80 dB | 4 ms | 60 ms |\n| VTL5C9 | 630 Ω @ 2 mA | 50 MΩ | **112 dB** | 4 ms | 50 ms |\n| VTL5C10 | 400 Ω @ 1 mA | 400 kΩ | 75 dB | 1 ms | 1.5 s |\n\n> 🐢 = fast-decay family (VTL5C1, C3, C6, C8, C9) — the \"pluck\" vactrols.\n> 🐌 = slow-decay family (VTL5C2, C4, C7, C10) — the \"swell\" vactrols.\n\n## 🎛️ How this maps to the sandbox module\n\nBoth `VTL5C3` and `VTL5C4` in the module browser share **one WASM DSP\nimplementation** (`native_modules/vactrol_envelope`) — they're not two\nseparate circuits, they're one envelope-follower parameterized differently,\nexactly like the real parts are one CdS-cell design binned into different\nspeed grades. 🏭\n\n| Knob (0–1 normalized, unchanged) | Readout shows | Real-world meaning |\n|---|---|---|\n| **Attack** | milliseconds | Time constant while the target gets *brighter* |\n| **Release** | milliseconds | Time constant while the target gets *dimmer* |\n| **Curve** | γ (LDR gamma) | Photoconductive exponent shaping the response |\n| **Sensitivity** | lux full-drive | Illuminance needed to hit 100% conductance |\n| **Light Offset** | lux bias | Ambient light added before the target is clamped |\n| **Dark Current** | kΩ dark R | Leakage / floor when the cell sees no light at all |\n\nThe knobs themselves stay plain 0–1 automation values (so patches, MIDI\nmapping, and macros all keep working normally) — only the **readout text**\ntranslates them into numbers a vactrol datasheet reader would recognize.\nSee [`node-graph-module-definitions.js`](public/node-graph-module-definitions.js)\nfor the `displayTransform` functions that do this.\n\n## 🧮 The DSP model\n\n```\ntarget       = clamp(light × sensitivity + lightOffset, 0, 1)\ncoefficient  = target > raw  ?  1 − e^(−1 / (attack  × sampleRate))\n                              :  1 − e^(−1 / (release × sampleRate))\nraw         += (target − raw) × coefficient\nshaped       = raw ^ curve\nout          = clamp(darkCurrent + shaped × (1 − darkCurrent), 0, 1)\n```\n\nThis is a standard **one-pole exponential follower with separate attack/\nrelease coefficients**, plus a power-law shaping stage (`raw ^ curve`) that\nstands in for the CdS cell's photoconductive gamma, and a floor term\n(`darkCurrent`) for the cell's non-zero dark leakage. It's the same overall\nshape used by the DAFx-2013 Buchla LPG model below, simplified for real-time\nuse. The native WASM build uses a fast bit-manipulation `pow()`\napproximation (accurate to a few percent — plenty for a *shaping curve*, not\na measurement instrument) since a `-nostdlib` freestanding build has no\n`libm` to call into.\n\n## 🎚️ Why LPGs love vactrols\n\nA **low-pass gate** (LPG) — the signature Buchla/Serge sound-shaping element\n— uses a *single* vactrol to simultaneously control **amplitude and filter\ncutoff** from one control signal, because both effects come from the exact\nsame physical resistance change. Turn down the light, the cell resists more,\nand *both* loudness and brightness fall together — the way a **struck,\ndamped physical object** naturally loses volume and brightness at the same\ntime. That's why LPG-driven plucks sound so organic compared to a separate\nVCA + VCF: it's one physical process, not two circuits pretending to agree.\n🔔➡️🔕\n\n## 📚 Scientific papers & primary sources\n\n- **PerkinElmer Optoelectronics** — *Low Cost Axial Vactrols VTL5C1, VTL5C2*\n  datasheet.\n  [farnell.com/datasheets/87223.pdf](https://www.farnell.com/datasheets/87223.pdf)\n- **PerkinElmer Optoelectronics** — *Low Cost Axial Vactrols VTL5C3, VTL5C4*\n  datasheet.\n  [Xvive/Synthrotek mirror](https://store.synthrotek.com/assets/images/XVIVE-VTL5C3-VTL5C4-Vactrol-Data-Sheet.pdf)\n- **PerkinElmer Optoelectronics** — *Analog Optical Isolators — VTL5C Series*\n  full-family summary table.\n  [uk-electronic.de/PDF/VTL.pdf](http://www.uk-electronic.de/PDF/VTL.pdf)\n- **Silonex** — NSL-32SR2 datasheet (a modern successor part in the same\n  photoresistor lineage after Silonex acquired the vactrol product line).\n- Parker, J., Esqueda, F., Bilbao, S. — *\"A Digital Model of the Buchla\n  Lowpass-Gate\"*, **Proc. of the 16th Int. Conference on Digital Audio\n  Effects (DAFx-13)**, Maynooth, Ireland, 2013. Measured a real VTL5C3/2 at\n  ~12 ms rise / ~250 ms decay — notably slower than the PerkinElmer spec,\n  likely due to unit-to-unit variance or a different-generation part; a good\n  reminder that datasheet *typicals* and *real components* diverge. 🔬\n- General background: **Kasap, S. O.**, *Optoelectronics and Photonics:\n  Principles and Practices* — standard reference for photoconductor gain,\n  persistence, and trap-state physics in CdS/CdSe cells.\n\n## 💬 Community references\n\n- [ModWiggler — \"Vactrols: slow or fast?\"](https://www.modwiggler.com/forum/viewtopic.php?t=58076)\n- [ModWiggler — \"Speed/decay-tail of VTL5C3/2 (particularly Xvive)\"](https://www.modwiggler.com/forum/viewtopic.php?t=222441)\n- [clsound.com — VC Resonant LPG build doc](http://clsound.com/vcresonantlpg.html)\n  (explicit VTL5C3 \"fast\"/VTL5C4-2 \"slow\" build guidance — the source for\n  calling these \"the\" standard fast/slow pair)\n- [modularsynthesis.com — hand-measured VTL5C3/VTL5C4 samples](https://modularsynthesis.com/vactrols/vactrols.htm)\n\n## ⚖️ A note on naming & IP\n\n`VTL5C3`/`VTL5C4` are PerkinElmer/Excelitas part numbers, used here purely\nas **descriptive, nominative labels** — the same way a pedal calling itself\n\"808-style\" or \"Moog-style\" is describing a sound, not claiming to *be* the\noriginal part. No PerkinElmer code, schematics, or datasheet artwork is\nreproduced anywhere in this repository; the DSP model and both diagrams on\nthis page are original work built from publicly published timing and\nresistance figures. This project is **not affiliated with or endorsed by\nPerkinElmer or Excelitas**. 🙏\n\n---\n\n*Made with 🔦, ☕, and an unreasonable amount of respect for a component*\n*that does audio-rate envelope following by literally glowing at itself.*\n\n---\n\n";
export const VACTROLS_REPO = "elanhickler/soemdsp-sandbox-vactrols";
